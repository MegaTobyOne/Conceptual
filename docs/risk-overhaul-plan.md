# Workshop Risk Overhaul Plan

Status: **implementation complete through Phase 4A; Phase 4B verification outstanding and now scheduled**
Last updated: 2026-09-20
Planning baseline: product `1.75.0`; compatibility axes `1.17.0`.
Next task: **Phase 4B - integrate and verify** (Phase 4A complete), scheduled as course-correction slice C1 (v1.77.0) per [../pspf-grand-plan.md](../pspf-grand-plan.md#course-correction-programme-v1760v1820) and [course-correction-plan.md](course-correction-plan.md). The 2026-09-19 deferral in favour of the Commitment-led programme is reversed. See [Phase 4B Handoff](#phase-4b-handoff).

C1 adds one requirement beyond the handoff below: the Risk surfaces are measured against the C0 interaction-cost instrument. A specialist workbench is not exempt from the flagship-journey cost ratchet.

## Authority and Outcome

This is the durable programme plan and session handoff, replacing the conversation's session-memory copy. The scope below is confirmed by the user; architecture below is resolved into [ADR 0098](../adr/0098-workshop-risk-overhaul-contract-baseline.md) (status `accepted`). Phases 0 through 4A have implemented the contracts, Core boundaries, score consumers, Workshop workbench, crosswalk flow and presentation outputs described in the records below. Phase 4B is still required before claiming verification-complete or release-sequencing readiness. Do not infer a separate release allocation or permission to publish from this document.

Follow the authority chain in [../pspf-spec-consistency-index.md](../pspf-spec-consistency-index.md) and sequencing in [../pspf-grand-plan.md](../pspf-grand-plan.md). Phase 0 must resolve architecture into an ADR before contract changes. Existing requirements remain in force until explicitly superseded.

Replace the minimal Risk form with a coherent Workshop editing and presentation workbench. Operators should be able to explain a risk, assess it using their organisation's methodology, connect it to enterprise risk, reuse treatments and controls, record escalation decisions, and present the result without maintaining duplicate records.

## Current state and latest learning

- Phases 0, 1A, 1B, 1C, 2, 3A, 3B and 4A are recorded as complete. The shipped baseline is product `1.75.0` with all three compatibility axes at `1.17.0`; the Risk schema slice remains published under `schemas/explorer-bundle/1.16.0` as intended by the compatibility plan.
- The full `pnpm run release:readiness` suite passed on 2026-09-08, including build, tests, E2E, accessibility, redaction, typecheck, lint, release-candidate checks, risk-source integration, and the Risk-specific gates. The readiness report was written to `.tmp/release-readiness/v1.75.0-readiness`.
- The readiness run exposed and fixed a stale `check-ownership-schema` assumption: that gate must validate the historical `1.16.0` ownership schema directory while validating the current `1.17.0` axes and standard fixture. The focused gate now passes 77 assertions and Prettier/lint remain green.
- The result is strong automated evidence, not a completed operator-verification claim. No live VS Code Extension Development Host walkthrough has yet exercised the end-to-end Risk journey, and the disclosed Phase 4A limitations remain: filtered output scope behaves like all, presentation-preset authoring is absent, external provenance is not rendered, and PNG output is a vetted monospace text render rather than a styled visual.

Recommended next step: execute Phase 4B as a verification slice. Start with the live create-to-card walkthrough in the Phase 4B Handoff, then run the accessibility/performance, redaction, compatibility and recovery evidence pass. Close or explicitly re-defer each disclosed limitation, update the affected specifications and gates, and only then decide separately whether release-sequencing work is authorised.

## Confirmed Scope

- Workshop only initially. No Explorer editor or visual overhaul; block incompatible publication with an explicit preflight rather than inventing legacy ratings.
- Editable category hierarchy **and** actual parent-risk relationships. These are separate concepts.
- Organisation-defined matrices, rating bands and risk appetite, not merely renamed fixed 5x5 labels.
- A register plus all five visuals: hierarchy tree, risk matrix, bow-tie, treatment coverage map and executive risk cards.
- Shared treatments and organisational controls, recorded escalation decisions, manual external-register references and previewed CSV/TSV crosswalk import.
- Preserve external systems of record. The Microsoft 365 examples are workflow guidance, not a technology requirement. No private register contents were supplied or fetched.

The suggested Enterprise / Digital / Cyber / Programme / Operational hierarchy is an optional editable template. Include Technology, Service Delivery, Infrastructure and Data under Digital; cyber themes such as vulnerability management, identity, external access, incident response and Essential Eight; and operational themes such as business process, workforce, supplier, compliance and information governance. Project names, committee names and ER8/ER10 are examples, never seeded factual risks or compulsory taxonomy.

## Current Implementation Anchors

Find symbols again when implementation begins; these observations describe the planning baseline, not permanent line numbers.

| Owning file                                                                                            | Relevant behaviour                                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [../packages/workshop/src/extension.ts](../packages/workshop/src/extension.ts)                         | `renderRiskEditor` edits title, status, likelihood and impact, then shows source/commercial context. Reuse `editorShell`, `recordWorkbenchShell`, `renderActionEditor`, `ownerTeamField`, `recordTable` and the Item Detail host. `openRisksList` currently uses an entity picker. |
| [../packages/contracts/src/index.ts](../packages/contracts/src/index.ts)                               | `RiskEntity` has a minimal fixed-score shape and 6clicks-specific `RiskIntegrationMetadata`. Actions already have owner team, due date and status. `OPERATOR_LINK_RULES`, entity registries and `PUBLICATION_FIELD_POLICIES` own shared contracts.                                 |
| [../packages/core/src/service.ts](../packages/core/src/service.ts)                                     | `prepareEntitiesForWrite` is the normal write-rule boundary; `upsertEntities` supports atomic batches. Import paths need equivalent validation. Export and team-share both use publication-sanitised collections.                                                                  |
| [../packages/workshop/src/continuous-compliance.ts](../packages/workshop/src/continuous-compliance.ts) | Some Workshop severity thresholds differ from the invariant four-band legacy scale. Strategy priority is a separate derived judgement, not an assessment band.                                                                                                                     |
| [../packages/workshop/src/workshop-ui.ts](../packages/workshop/src/workshop-ui.ts)                     | Reuse pure UI adapters and escaped rendering patterns, with nearby runtime tests.                                                                                                                                                                                                  |
| [../packages/webview-shell/src/index.ts](../packages/webview-shell/src/index.ts)                       | Reuse theme, shell and relationship primitives rather than creating a second design system.                                                                                                                                                                                        |
| [../packages/connected-view/src/index.ts](../packages/connected-view/src/index.ts)                     | Reuse appropriate graph read-model and selection patterns without resurrecting retired global views.                                                                                                                                                                               |

Canonical `risk -> treated-by -> action` semantics exist, but Risk authoring picker coverage must be checked and completed. Change Record direction is `change-record -> changes -> risk`, not the reverse. Vendored ISM source controls are not writable organisational control definitions.

The Workshop `exportBackupJson` command delegates to Core publication export. It must **not** be treated as proof of lossless recovery for new sensitive fields. The sanitiser rejects unknown fields and has a specific public projection for risk source metadata; do not generalise that exception to new provenance fields.

## Proposed Data Design

The entity names, link representation, matrix bounds and state machines in this section are candidates for Phase 0, not decisions already made by the user.

### Risk and Hierarchy

- Extend Risk with an operator reference separate from its canonical ID, structured causes/event/consequences, optional description, primary category, owner team, review date, assessments, response and source provenance.
- Allow minimal capture with title and an explicit unassessed state. Missing scores never mean zero or Low. Preserve open/monitored/closed lifecycle; acceptance and escalation are separate decisions.
- Prefer one primary parent and separately typed secondary enterprise associations. Candidate representation: `child risk -> rolls-up-to -> parent risk`, with explicit allowed triples, cardinality and cycle validation. Do not also maintain an independently editable `parentRiskId` or enterprise ancestor field.
- Categories are stable-ID configuration nodes, not Risk entities and not replacements for the six PSPF Domains. One primary category gives deterministic appetite inheritance. Allow rename, reorder and archive; prevent deletion of referenced nodes and preview category moves that affect appetite.
- Actual risk depth is not dictated by categories. Programme, cyber or operational risks may contribute to appropriate enterprise risks without a compulsory intermediate record.

### Assessment and Appetite

- Propose a Core-owned risk-framework configuration record containing taxonomy, source-register definitions, methodology/appetite revisions and explicit presentation presets. Configuration is workspace data, not user settings.
- Start with bounded declarative matrices: candidate bounds are 2-10 ordered likelihood levels and 2-10 ordered impact levels. Every cell has a named ordered band; numeric cell scores are optional. No executable formulas or arbitrary scripts.
- Seed a legacy 5x5 methodology matching the invariant bands: below 5, 5-9, 10-15, and 16 or more. Preserve existing assessments with their basis identified as legacy until an operator confirms what they represent.
- Current means assessed residual risk with existing controls; inherent is optional before-controls risk; target is intended after-treatment risk, not realised improvement. Store assessment date, rationale and methodology revision.
- Pin assessments to immutable activated revisions. A methodology revision ID is provenance, not a fourth compatibility axis. New revisions do not silently re-rate historical records; provide preview and explicit reassessment.
- Propose appetite as allowed cells with rationale, effective date and review date. The nearest explicit primary-category rule overrides the workspace rule. No rule means Appetite not set. Acceptance exceptions are recorded decisions, not silent appetite changes. Resolve expired-rule behaviour in Phase 0.
- Use a discriminated legacy/custom assessment contract, not dummy mandatory legacy numbers. Shared evaluation should distinguish assessed, unassessed, legacy basis and not comparable, with an explanation and methodology reference.
- Never combine incomparable methodologies on a single numeric scale. Audit multiplication and threshold consumers across contracts, Core summaries/snapshots, Workshop, reports, Connected View and Shop. Adapt them or explicitly disclose unsupported inputs and excluded counts. Necessary Shop changes are correctness guards only; no Shop overhaul.

### Treatments, Controls and Bow-Tie

- Reuse canonical Actions as treatments through `treated-by`, with one owner, due date and status for each shared Action. Provide create/link/reuse and affected-risk preview. Do not introduce a parallel treatment register with duplicated Action state.
- A risk-level response such as reduce, avoid, share or accept is distinct from treatment progress.
- Propose an organisational control entity, provisionally `risk-control`, with definition, team owner, state, review date and evidence. It may reference ISM source controls but never modifies or duplicates the vendored catalogue.
- Represent risk-specific control applications explicitly: preventive/recovery/both role, applicability, effectiveness, rationale, evidence and cause/consequence anchors. Phase 0 must decide whether typed link metadata or a dedicated application record best fits current validation.
- Causes and consequences have stable IDs inside the Risk. Bow-tie applications must reject missing anchors. Action-to-control-gap relationships need explicit allowed triples.
- Count distinct treatment/control IDs; distinguish direct from descendant coverage and disclose shared attribution. Attaching a control or finishing an Action never reduces a rating automatically. Evidence presence alone never establishes control effectiveness.

### History and Escalation

- Propose Core-owned risk-event records for reassessment, reparenting, reconciliation and escalation. Append factual events atomically with the mutation; clients cannot rewrite them. No-op saves produce no false events.
- Existing Change Records can hold significant decision narratives with `changeType: risk-response` and the correct `changes` direction. They are not a competing factual audit stream.
- Candidate escalation states: proposed, accepted, declined and withdrawn. Capture source risk, destination risk and/or governance body/team label, reason, dates, disposition and treatment/decision references. Corrections append subsequent events.
- Escalation does not automatically change category, move, duplicate, close or re-rate a risk. Accepted escalation may offer a separately previewed and confirmed parent/category change.
- Parent ratings remain independently assessed. Summarise child bands, unassessed/over-appetite counts and overdue treatments, never sums or averages of ordinal ratings.
- Recovery/undo remains explicitly governed; event history is traceability, not a claim of cryptographic tamper resistance.

### External Registers

- Distinguish local risks from local representations of externally authoritative records. Source-owned assessment fields and local relationships/notes must remain separate.
- Identify records by source register plus external ID. Preserve original external rating/scale, source dates, reconciliation provenance and safe reference URLs. Do not convert external scores automatically or permit silent source-field replacement through ordinary editing.
- Preserve existing 6clicks metadata and workflows while bridging recognised records to generic references without duplicate identities. Do not change authentication or add network dependencies.
- CSV/TSV import uses a suitable maintained parser, a field template, column mapping, source selection and validation. Preview create/reuse/update/conflict/unmatched counts and require explicit confirmation.
- Match by stable source identity. Title similarity may suggest duplicates but never merges them. Stage unresolved crosswalk rows outside the live graph; reject cycles and dangling references in the proposed final graph.
- Revalidate against the current store after preview, commit atomically with a recovery checkpoint, and prove repeat imports are idempotent. Preserve local notes and relationships; absent source rows do not imply deletion.

## Workbench and Presentation

Reuse existing Risk commands and the Item Detail panel implementation. The list command should open the register state of this workbench, including when empty. No new top-level route, panel or command is planned; preserve the Essentials surface budget.

Use a stable record/context rail and unframed working area, with compact ID/status/owner/current/target/appetite summary, Save/Cancel and sections for Record, Assessment, Treatments and controls, Relationships and History. Preserve drafts across view switches; make failed, no-op and successful saves distinct. Writer/trust read-only state must disable mutations, and source-owned fields must be visibly distinguished.

| View      | Recording and presentation control                                                                                                                                                                                                                     |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Register  | Search; category/source/team/status/appetite/review filters; grouping/sort; resizable columns and density. Preserve usable title/reference widths. Preview bulk owner/category/review assignments; no inferred bulk ratings.                           |
| Hierarchy | Separate category/risk modes, expandable branches, ancestor breadcrumb, primary edges distinct from secondary associations, direct/descendant counts and keyboard reparent picker with preview.                                                        |
| Matrix    | Methodology/revision and inherent/current/target selection; appetite boundaries; counts and cell drill-down; comparable current-to-target markers. Keep unassessed/unmapped counts outside the grid. No silent drag-to-rate or mixed-methodology grid. |
| Bow-tie   | Causes, preventive controls, event, recovery controls and consequences; evidence/action drill-in and visible gaps. Selected-risk view within detail, not a standalone dashboard.                                                                       |
| Coverage  | Focused many-to-many risks/actions/controls diagram with filtering, selection and an equivalent semantic table. Reuse appropriate graph tooling; lazy-loaded 2D only.                                                                                  |
| Cards     | Configurable fields, section order, density and scope. Retain ID/title, methodology/basis, as-of time and classification in outputs. Show owner, next decision and treatment progress only when policy permits.                                        |

Keep transient view state local. Explicit named presets may persist as policy-declared configuration, without expanding Explorer SavedView scopes. Presets change presentation, never facts, calculations or publication rules. Configuration is an internal workbench view, not another product dashboard.

Provide plain text/Markdown copy, copy/save PNG for visuals and self-contained print HTML. Render from a common allowlisted output DTO with preview, scope/filter/source/methodology/as-of cues and classification. Explicitly opt in sensitive fields by name under an approved policy; never use wildcard permission or raw screenshots of sensitive editor DOM. Native Office and PDF generation are excluded.

Follow current VS Code theme tokens and shared compact controls, AU English, clear focus and non-colour labels. All diagrams need table/keyboard alternatives and narrow-layout equivalents. Avoid cramped chips, nested cards, role lenses, free-form diagram editors and retired 3D surfaces.

## Compatibility and Recovery

- Before any publication files/directories are written, Core preflight must reject representations the supported Explorer cannot truthfully consume. Cover both master export and team-share, unsupported ratings/entities/links and incompatible axes.
- Do not fabricate legacy scores, silently omit risks, drop relationships or report partial success. User-confirmed partial publication is a separate future decision, not part of this plan.
- Preserve supported legacy publication where compatible. An axis bump can make even legacy subsets incompatible with older Explorer versions; preflight must explain this, not assume backwards compatibility.
- Every new field, including nested configuration and output fields, needs explicit policy; default to sensitive. New titles/structure are not automatically public because an older entity envelope was public. Use team/governance labels, never person ownership fields. Exclude sensitive values from notifications, status bars, quick-pick/tree labels and logs as required by policy.
- Explain or block misleading JSON backup behaviour for the new model. Before migration, prove lossless local database plus required metadata backup/restore, excluding runtime locks. Do not introduce another exchange format or describe publication JSON as complete recovery.
- Prove allowed risk data and history survive snapshots and cold restore, with fresh writer ownership. Restricted information remains excluded from snapshot/export artefacts. Any portable sensitive backup profile needs its own explicit policy decision.
- Protect new fields/history from stale Explorer writes. Preserve server-owned fields on supported updates; reject incompatible custom/stale updates. Full-replace must disclose data loss, require appropriate confirmation and retain a recovery checkpoint. Crosswalk import is not an Explorer import bypass.

## Phases and Model Assignments

Use the strongest available version of the named model family. These are recommendations, not repository benchmarks or verified pricing; check current picker availability and request multipliers. Opus is recommended for cross-package/trust-boundary decisions, Sonnet for bounded implementation against settled contracts. Small/fast models such as Haiku or mini-class should not lead or solely review this programme; limit them to precisely specified mechanical follow-up with validation.

| Phase                                | Dependencies                     | Lead model                                                     | Deliverable and exit gate                                                                                                                                                              | Progress                                                                       |
| ------------------------------------ | -------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 0: Contract and interaction baseline | None                             | Claude Opus                                                    | ADR proposal and decision closure; field/link/policy tables; compatibility and recovery strategy; baseline evidence and workbench storyboard. No production contract changes.          | Done 2026-09-07 (ADR 0098 accepted; all decisions closed as recommended)       |
| 1A: Shared model                     | Accepted Phase 0 decisions       | Claude Opus                                                    | Assessment/framework/hierarchy/control/event contracts; shared evaluator and validators; legacy adapters and focused tests. Batch schema changes in one planned compatibility release. | Done 2026-09-07                                                                |
| 1B: Persistence and exchange         | 1A                               | Claude Opus                                                    | Core atomic validation/history, import protection, explicit migration, publication preflight and verified recovery. All mutation entry points covered.                                 | Done 2026-09-07 (schema axis bumped to `1.17.0`, slice `1.75.0`)               |
| 1C: Score consumers                  | 1A; must finish before 2         | Claude Opus                                                    | Replace or guard legacy arithmetic; no fake zero/bands/priority, NaN or hidden exclusions. Can develop alongside 1B only with non-overlapping ownership.                               | Done 2026-09-07                                                                |
| 2: Record and assess                 | 1B and 1C                        | Claude Sonnet                                                  | Structured editor/register/configuration plus hierarchy and matrix; behavioural, keyboard and visual checks. Escalate contract gaps to Opus.                                           | Done 2026-09-07 (no live VS Code walkthrough; see Phase 2 Record disclosures)  |
| 3A: Treat, explain, escalate         | 2                                | Claude Sonnet; Opus review                                     | Shared Actions/controls/applications, bow-tie, coverage and escalation history; Opus reviews event integrity and aggregation.                                                          | Done 2026-09-07 (no live VS Code walkthrough; see Phase 3A Record disclosures) |
| 3B: Source crosswalk                 | 1B and 2                         | Sonnet UI/parser orchestration; Opus mutation semantics/review | Manual references and previewed CSV/TSV import; authority separation, conflict, idempotence and rollback tests.                                                                        | Done 2026-09-07 (no live VS Code walkthrough; see Phase 3B Record disclosures) |
| 4A: Present                          | 2; final integration needs 3A/3B | Claude Sonnet; Opus publication review                         | Cards, presets and safe text/image/HTML outputs; provenance and field-policy evidence.                                                                                                 | Done 2026-09-07 (no live VS Code walkthrough; see Phase 4A Record disclosures) |
| 4B: Integrate and verify             | 3A, 3B and 4A                    | Claude Opus                                                    | Full operator walkthrough, accessibility/performance, redaction, compatibility and recovery evidence; truthful documentation and gate updates.                                         | Not started                                                                    |

No release number, ADR number or new axis is reserved here. Allocate the next appropriate values during governance work; preserve published schema directories and the three compatibility axes. Do not assume optional fields avoid schema review. Read the developer pipeline spec before release/gate-wiring changes, and obtain separate authorisation to publish.

## Phase 0 Handoff

Start a new Opus session for Phase 0 only. Initial task:

> Read docs/risk-overhaul-plan.md and the repository instructions. Execute Phase 0 only: verify the local baseline, resolve the proposed Risk architecture into an ADR proposal and explicit decision tables, document the current editor/scoring/publication/recovery behaviour, and prepare a bounded Phase 1A handoff. Preserve the confirmed Workshop-only scope and unchanged Explorer interface. Do not implement production entities, migrations, new visual surfaces, version bumps or release operations. Do not mark an ADR accepted merely because it has been drafted; identify decisions needing operator approval. Stop after Phase 0 verification and report the next exact task.

Before work, read [AGENT_ORIENTATION.md](AGENT_ORIENTATION.md), [../pspf-grand-plan.md](../pspf-grand-plan.md) and [../pspf-spec-consistency-index.md](../pspf-spec-consistency-index.md). For contract decisions, consult [../pspf-entity-link-spec.md](../pspf-entity-link-spec.md), [../pspf-core-api-contract-spec.md](../pspf-core-api-contract-spec.md), [../pspf-core-workshop-screen-workflow-spec.md](../pspf-core-workshop-screen-workflow-spec.md) and [../pspf-invariants.md](../pspf-invariants.md). For boundary decisions, consult [../pspf-security-redaction-controls.md](../pspf-security-redaction-controls.md), [../adr/0005-redaction-default-deny.md](../adr/0005-redaction-default-deny.md), [../pspf-explorer-json-bundle-schema-spec.md](../pspf-explorer-json-bundle-schema-spec.md), [../pspf-migration-safety-runbook.md](../pspf-migration-safety-runbook.md) and [../pspf-backup-and-restore-runbook.md](../pspf-backup-and-restore-runbook.md). Read [../pspf-acceptance-and-quality-gates.md](../pspf-acceptance-and-quality-gates.md) before claiming completion.

Phase 0 decisions requiring explicit closure:

1. Exact legacy/custom/unassessed Risk shape, what old scores mean, and how to represent the unsupported-comparison state in every derived consumer.
2. Framework/category/methodology/appetite persistence, matrix limits, revision/expiry semantics, primary category and reclassification rules.
3. Parent/secondary relationship direction, cardinality, cycle detection and control-application representation. Use a single source of truth for each relationship.
4. Event authority, escalation states, correction/undo/restore rules and validation on every import/write path.
5. Field-level and nested publication policy, explicitly permitted output profiles, and boundary checks without Explorer implementation changes.
6. Compatibility-axis evolution and which existing export/import workflows will be blocked, including legacy-only data under a newer schema.
7. Verified recovery path before migration; the distinction between publication JSON and a lossless backup; consent and rollback semantics.
8. Precise no-growth Workshop navigation, first usable slice, baseline fixtures and measurable performance/accessibility criteria.

Phase 0 is complete only when the baseline evidence and storyboard are recorded, decisions are accepted or explicitly marked as blockers, authoritative documents are coordinated, and Phase 1A has a narrow file/test scope. An unresolved blocking decision prevents Phase 1A; it does not prevent recording a useful partial Phase 0 handoff.

## Phase 0 Record

Executed 2026-09-07 with Claude (Opus-class) in a single session. Output: [ADR 0098](../adr/0098-workshop-risk-overhaul-contract-baseline.md) (`proposed`), this record, and the coordination edits listed under Files. No production contract, Core, Workshop or schema code was changed.

### Baseline evidence

Environment: HEAD `a99d3c2` on `develop`; Node `v22.23.1`; pnpm `10.10.0`. Pre-existing uncommitted worktree changes (`pspf-grand-plan.md`, `pspf-spec-consistency-index.md`, this file) were preserved.

| Check                                                              | Result                                                               |
| ------------------------------------------------------------------ | -------------------------------------------------------------------- |
| `pnpm build`                                                       | pass (all packages)                                                  |
| `pnpm run check:risk-source-integration`                           | pass                                                                 |
| `pnpm run check:schema-policy`                                     | pass                                                                 |
| `pnpm run check:essentials-surface`                                | pass: routes 24/24, nav 18/18, Workshop commands 72/72, panels 30/30 |
| `pnpm run check:adr-coverage`                                      | pass before ADR 0098 (97 ADRs); re-run after registration, see below |
| `node --test packages/workshop/dist/continuous-compliance.test.js` | 11/11 pass                                                           |

Not run in Phase 0: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `release:readiness`. They are unaffected by documentation-only edits and belong to the Phase 1A slice.

### Current behaviour (verified by reading source at the baseline)

- **Editor.** `renderRiskEditor` (`packages/workshop/src/extension.ts`) renders title, status, likelihood and impact selects inside `editorShell`, then a read-only 6clicks source section and the commercial context section, all inside `recordWorkbenchShell`. Save coerces the two scores with `Number()` and rejects anything outside whole numbers 1-5. `openRisksList` is a quick pick over non-deleted Risks, then `openEntityEditor`. `createRisk` prompts title, status, likelihood, impact and a mandatory requirement selection, then writes the Risk plus `requirement -> exposed-by -> risk` links.
- **Validation.** Core `validateEntityWriteRules` has no Risk rule; `importBundle`/`buildImportPlan` do not call `prepareEntitiesForWrite`. The publication sanitiser throws for any field without a declared policy, which is the only fail-closed guard for new fields.
- **Scoring.** Seven consumers compute `likelihood × impact` independently with three different band tables and two different missing-value behaviours; the table in ADR 0098 § Context lists each. The Workshop tree label (`>=4` Medium) and the three-band `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES` diverge from invariant E5.
- **Links.** `treated-by` exists in `LINK_TYPES` and is read by Connected View, but has no `OPERATOR_LINK_RULES` entry and no Workshop authoring path. Workshop can author only `exposed-by` into Risk; Shop authors `supplier -> associated-with -> risk`.
- **Publication.** All Risk fields public except `integration` (projected to `sourceLabel`/`remoteUpdatedAt`). `schemas/explorer-bundle/1.16.0/collections/risks.schema.json` requires integer `likelihood`/`impact` 1-5; an unassessed Risk is unpublishable under the current schema.
- **Recovery.** Workshop `exportBackupJson`/`importBackupJson` delegate to Core publication export/import; they are sanitised bundles, not lossless backups. Lossless backup is the SQLite database plus `.pspf/config/*.json`, excluding locks, per the runbook. Full-replace and additive imports write a `pre-<importId>.json` undo point.
- **Documentation gap.** `pspf-entity-link-spec.md` § Risk describes an unimplemented shape (string bands, `riskType`, `residualLevel`, `treatmentStatus`, `ownerPersonId`, `acceptedBy`) that contradicts the implementation and ADR 0097 D5. ADR 0098 supersedes that section; Phase 1A corrects the spec.

### Workbench storyboard (Phase 2 target, within existing surfaces)

1. **Open.** `pspf.workshop.openRisksList` opens the Item Detail panel in **Register** state (search, filters, grouped table, "New risk", "Set up risk framework" when no framework exists). Empty state explains the two ways to get a Risk: create one, or import from a source register.
2. **Record.** Selecting a row swaps the working area to the record: header rail with ID, reference, status, owner team, current/target band and appetite state; sections Record (title, description, primary category, causes, consequences, response, review by), Assessment, Treatments and controls, Relationships, History. Save/Cancel are sticky; a dirty draft survives switching between sections and views.
3. **Assess.** Assessment section shows basis (`Legacy 5×5`, custom methodology name and revision, or `Unassessed`). For legacy: the two existing selects. For custom: matrix picker per current/inherent/target with band and appetite read-back and rationale. Changing a methodology revision never re-rates; it offers "Reassess against r2" as an explicit action.
4. **Treat.** Treatments list linked Actions (owner team, due, status) with "Link existing action" (picker filtered to open Actions, showing other Risks each Action already treats) and "Create action" (existing `renderActionEditor`). Controls list `risk-control` applications with role and effectiveness; bow-tie is a tab inside this section.
5. **Relate.** Relationships shows primary parent (one row, "Change parent" opens a picker with cycle-safe candidates and a roll-up preview), secondary associations, exposed requirements/ISM controls, suppliers/contracts and change records.
6. **Escalate.** History lists Core-derived events newest first. "Record escalation" opens an inline form (destination Risk or governance label, reason, state) and appends an `escalation` event; on `accepted` it offers, separately, a previewed reparent/reclassify.
7. **Present.** Register toolbar "Views" switches Register / Hierarchy / Matrix / Coverage / Cards; each has a table alternative and copy/save actions that render from `RiskOutputModel` with classification and as-of cues.
8. **Publish.** Export/team-share from Workshop runs Core preflight first; a blocked result lists the offending Risk IDs and the reason, with no files written.

### Decisions

Accepted without further design: D1.1-1.5, D2.1-2.6, D2.8, D3.1-3.4, D3.6-3.8, D4.1-4.6, D5.1-5.4, D6.1-6.4, D7.1-7.3, D8.1-8.4.

Formerly blocking, closed by the operator on 2026-09-07 as recommended:

- **D1.6 / D6.5** — publication of custom-basis and unassessed Risks is blocked at preflight in this programme; no schema change to make `likelihood`/`impact` optional.
- **D2.7** — an expired appetite rule still applies and is flagged stale ("Appetite review overdue").
- **D3.5** — typed `LinkEntity.application` metadata for control applications; no dedicated entity.

Deferred to a later ADR: portable sensitive backup profile (D7.4); Explorer-aware publication of custom assessments.

### Files changed in Phase 0

- `adr/0098-workshop-risk-overhaul-contract-baseline.md` (new; `proposed`, then `accepted` the same day after operator closure).
- `adr/README.md` (index row), `scripts/check-adr-coverage.mjs` (manual coverage entry naming existing gates; no gate logic changed).
- `docs/risk-overhaul-plan.md` (this record, progress table, handoff), `pspf-grand-plan.md` and `pspf-spec-consistency-index.md` (pointer to ADR 0098).

## Phase 1A Handoff

ADR 0098 is accepted, so Phase 1A may start. Initial task for a fresh Opus session:

> Read docs/risk-overhaul-plan.md § Phase 1A Handoff, adr/0098-workshop-risk-overhaul-contract-baseline.md and the repository instructions. Execute Phase 1A only: add the Risk overhaul contracts and the shared evaluator to `@pspf/contracts` with focused node tests, correct `pspf-entity-link-spec.md` § Risk, and declare publication policy for every new field. Do not change Core persistence, Workshop UI, published schemas, `VERSION_AXES` or `PSPF_SLICE_VERSION`. Stop after `pnpm --filter @pspf/contracts build && pnpm --filter @pspf/contracts test`, `pnpm run check:schema-policy` and `pnpm typecheck` pass, and report the Phase 1B handoff.

Bounded file scope:

| File                                                         | Change                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/contracts/src/risk-model.ts` (new)                 | `RiskAssessment` union, `RiskFrameworkEntity`, `RiskControlEntity`, `RiskEventEntity`, `RiskControlApplication`, `evaluateRisk`, `resolveAppetite`, `validateFramework`, `detectRollUpCycle`, `LEGACY_5X5_METHODOLOGY` encoding E5. Pure, deterministic, no I/O.                                                                                                                                                              |
| `packages/contracts/src/risk-model.test.ts` (new)            | Band boundaries 4/5/9/10/15/16; unassessed and not-comparable never yield a band; 3×3, 5×5, 2×10 matrices; invalid/missing cells rejected; revision immutability; appetite inheritance, expiry (per D2.7 outcome) and "not set"; cycle and self-link detection.                                                                                                                                                               |
| `packages/contracts/src/index.ts`                            | Add optional Risk fields from the ADR table; add `"risk-framework"`, `"risk-control"`, `"risk-event"` entity types, ID prefixes `RFW`/`RCT`/`RSE`, collection names; add `"rolls-up-to"`, `"mitigated-by"` to `LINK_TYPES`; `LinkEntity.linkRole?` and `LinkEntity.application?`; `OPERATOR_LINK_RULES` rows from the ADR table; `PUBLICATION_FIELD_POLICIES` entries for every new field and entity; re-export `risk-model`. |
| `packages/contracts/src/index.test.ts` (or nearest existing) | Sanitiser strips every new sensitive field and keeps `assessmentState`/`primaryCategoryId`; every new entity type has a policy; `OPERATOR_LINK_RULES` contains `workshop-risk-treated-by-action`.                                                                                                                                                                                                                             |
| `pspf-entity-link-spec.md` § Risk                            | Replace the unimplemented shape with the ADR 0098 field and link tables; remove `ownerPersonId`/`acceptedBy`.                                                                                                                                                                                                                                                                                                                 |
| `pspf-invariants.md`                                         | Add the new entity type strings, collection names and ID prefixes to N1-N3; leave E5 unchanged.                                                                                                                                                                                                                                                                                                                               |

Explicit exclusions for 1A: no Core `service.ts` change (1B), no consumer rewrites (1C), no schema directory, no version or axis bump, no Workshop or Explorer change, no gate registration beyond `check:schema-policy` continuing to pass. Explorer's `riskBandOf` stays untouched. If `check:schema-coverage` or `check:explorer-publication` fail because a new entity type has no schema file, record it as the expected 1B input rather than adding a schema in 1A.

## Phase 1A Record

Executed 2026-09-07 with Claude (Opus-class) in a single session, immediately following Phase 0 in this repository state.

### Files changed

- `packages/contracts/src/risk-model.ts` (new): `RiskAssessment` union (`unassessed`/`legacy`/`custom`), `RiskFrameworkEntity`, `RiskControlEntity`, `RiskEventEntity`, `RiskControlApplication`, `RiskCauseOrConsequence`, `RiskExternalRef`, `RiskResponse`, `evaluateRisk`, `resolveAppetite`, `validateFramework` (with revision-immutability check via an optional `previous` framework), `detectRollUpCycle`, `validateRollUpEdges`, `validateControlApplicationAnchors`, `LEGACY_5X5_METHODOLOGY` encoding invariant E5 exactly. Pure, deterministic, no I/O.
- `packages/contracts/src/risk-model.test.ts` (new, 34 tests): legacy band boundaries at scores 4/5/9/10/15/16; unassessed and not-comparable never carry a band or score; 3×3, 5×5 (seeded) and 2×10 matrices valid; missing cell, unknown band, and out-of-range level count rejected; an activated revision cannot be mutated in place but an unactivated one can, and a new revision ID beside an activated one is not a mutation; appetite nearest-category-overrides-workspace, parentId walk-up, over-appetite, D2.7 stale-but-still-applies, and "not set" with no framework/no rule; roll-up self-link and multi-hop cycle detection, plus dangling-endpoint and multiple-parent validation; control-application anchor resolution against `causes`/`consequences`.
- `packages/contracts/src/index.ts`: added `"risk-framework"`/`"risk-control"`/`"risk-event"` to `V0_1_ENTITY_TYPES`/`V0_1_COLLECTIONS`/`COLLECTION_BY_ENTITY_TYPE`/`ID_PREFIX_BY_ENTITY_TYPE` (`RFW`/`RCT`/`RSE`) and the `V01Entity`/`EntityByCollection` unions; added `"rolls-up-to"`/`"mitigated-by"` to `LINK_TYPES`; added the ADR field table to `RiskEntity` (`reference`, `description`, `causes`, `consequences`, `primaryCategoryId`, `ownerTeam`, `reviewBy`, `assessment`, `assessmentState`, `response`, `externalRefs`; `likelihood`/`impact`/`integration` unchanged) and `linkRole?`/`application?` to `LinkEntity`; added five `OPERATOR_LINK_RULES` rows (`workshop-risk-rolls-up-to-risk`, `workshop-risk-related-to-risk`, `workshop-risk-treated-by-action`, `workshop-risk-mitigated-by-risk-control`, `workshop-action-addressed-by-risk-control`); updated the `risk`/`link` `PUBLICATION_FIELD_POLICIES` entries and added entries for the three new entities (all non-envelope fields `sensitive`; `primaryCategoryId`/`assessmentState`/`linkRole` are the only new `public` fields); re-exported `risk-model.ts`.
- `packages/contracts/src/operator-link-rules.test.ts` and `packages/contracts/src/publication-policy.test.ts` (extended, not a new `index.test.ts`): cover the five new link rules, every new entity type having a policy, the sanitiser stripping every new sensitive Risk/Link field while keeping `assessmentState`/`primaryCategoryId`/`linkRole`, and the three new entities publishing only structural envelope fields.
- `pspf-entity-link-spec.md` § Risk: replaced with the ADR 0098 field/enum/assessment/external-reference tables; removed `riskType`, `residualLevel`, `treatmentStatus`, `ownerPersonId`, `acceptedBy`; added a short cross-reference to the three new entities (full detail stays in ADR 0098, per the bounded scope).
- `pspf-invariants.md`: added `risk-framework`/`risk-control`/`risk-event`, `risk-frameworks`/`risk-controls`/`risk-events`, and `RFW`/`RCT`/`RSE` to N1/N2/N3. E5 untouched.
- **Deviation from the bounded exclusion list, disclosed here:** `packages/core/src/service.ts`, two lines only. Widening `BundleCollections`/`EntityByCollection` in contracts made Core's two exhaustive collection-literal builders — `createEmptyCollections()` and `getCollectionCounts()` — fail to compile (`error TS2739: missing ... "risk-frameworks", "risk-controls", "risk-events"`). `check:schema-policy` runs `pnpm build` for the whole workspace first, and `pnpm typecheck` builds all project references, so both stop-gate commands fail without this fix. The fix adds three `[]` entries to `createEmptyCollections()` and three `.length` entries to `getCollectionCounts()` — no write path, validation rule, migration, or import/export behaviour was touched. Reviewers should diff exactly these two literals in `service.ts` and confirm nothing else in Core changed.

### Commands and results

- `pnpm --filter @pspf/contracts build` — pass.
- `pnpm --filter @pspf/contracts test` — 169/169 pass (135 pre-existing + 34 new in `risk-model.test.ts`, plus the extended assertions in `operator-link-rules.test.ts`/`publication-policy.test.ts`).
- `pnpm run check:schema-policy` — pass (runs a full `pnpm build`, which now includes the Core fix above).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).

### Known follow-ups for later phases

- The three new collections (`risk-frameworks`, `risk-controls`, `risk-events`) have no `schemas/explorer-bundle/1.16.0/collections/*.json` files and no schema-version bump yet. `check:schema-coverage` and `check:explorer-publication` were **not run** in this session (they are outside the Phase 1A stop-gate list) and are expected to fail until Phase 1B lands the schema bump named in D6.1/D6.2 — this is the expected 1B input, not a regression.
- `pnpm lint`, `pnpm build` (full), `pnpm test` (full monorepo), and `release:readiness` were not run; only the four commands named in the Phase 1A stop condition were run, plus the targeted Core rebuild needed to diagnose the deviation above.
- No fixtures, sample workspace data, or Core write-rule/import-plan changes exist yet for the three new entities or `assessment`/`rolls-up-to`/`mitigated-by`; `evaluateRisk`/`resolveAppetite`/`validateFramework`/`detectRollUpCycle` are not called from any product code yet.

## Phase 1B Handoff

Phase 1A is complete: the contracts, evaluator and docs above are in place and green against their stop gates. Initial task for a fresh Opus session:

> Read docs/risk-overhaul-plan.md § Phase 1B Handoff, the Phase 1A Record immediately above it, adr/0098-workshop-risk-overhaul-contract-baseline.md and the repository instructions. Execute Phase 1B only: wire Core atomic validation and history for the Risk overhaul contracts landed in Phase 1A (write-rule validation, Core-derived `risk-event` authoring, `rolls-up-to`/`mitigated-by` cycle and cardinality checks reusing `@pspf/contracts`' `validateRollUpEdges`/`validateFramework`/`validateControlApplicationAnchors`), the explicit migration command that seeds `risk-framework` with `LEGACY_5X5_METHODOLOGY`, import protection (`buildImportPlan`/`mergeIncomingEntity` per D6.4), the new publication preflight (D6.3) that blocks `custom`/`unassessed` Risks and any `rolls-up-to`/`mitigated-by` link, the schema bump (D6.1/D6.2: new collection schemas, updated Risk/Link schemas, next `schemaVersion`/`bundleVersion`/`apiVersion`), and the cold-restore proof (D7.1). Do not touch Workshop UI, Explorer, or Phase 1C consumer rewrites. Stop after the checks named in the Phase 1B exit gate below pass, and report the Phase 1C handoff.

Carried forward from Phase 1A, for Phase 1B to consume directly rather than reinvent:

- `evaluateRisk(risk, framework)`, `resolveAppetite(risk, framework, evaluation, now)`, `validateFramework(framework, previous?)`, `detectRollUpCycle(edges)`, `validateRollUpEdges(edges, knownRiskIds)`, `validateControlApplicationAnchors(risk, application)`, `LEGACY_5X5_METHODOLOGY` — all in `packages/contracts/src/risk-model.ts`, re-exported from `@pspf/contracts`.
- The five new `OPERATOR_LINK_RULES` rows and the `RiskEntity`/`LinkEntity` field additions are typed and policy-declared; Phase 1B adds the runtime write-rule enforcement and event derivation, it does not need to add new contract fields.
- `PUBLICATION_FIELD_POLICIES` already covers every new field/entity; Phase 1B's preflight (D6.3) is a **separate, additional** block on top of the sanitiser, not a replacement for it.

Known open items Phase 1B must resolve (not resolved by Phase 1A):

- Schema-version target allocation (D6.1 says "not here"); `check:schema-coverage`/`check:explorer-publication` currently fail for the three new collections and must be fixed as part of this phase's own exit gate, not treated as pre-existing red.
- `createSampleEntities`/home-user sample fixtures (D8.3) do not yet include a framework, category template, roll-up chain, shared Action, `risk-control`, or escalation event; Phase 1B likely needs at least minimal fixtures to exercise the new write/import paths in its own tests, even though the full fixture set is nominally D8.3/Phase 2 scope.
- The Core `service.ts` deviation recorded above (two literal-completeness edits) is the only Core code that exists for the new entities; Phase 1B is expected to add real read/write/validation logic around them.

Stop after: `pnpm --filter pspf-core build && pnpm --filter pspf-core test`, `pnpm --filter @pspf/contracts test`, `pnpm run check:schema-coverage`, `pnpm run check:schema-policy`, `pnpm run check:explorer-publication`, and `pnpm typecheck` all pass, plus the D7.1 cold-restore proof is executed against a fixture workspace with evidence recorded in this document.

## Phase 1B Record

Executed 2026-09-07 with Claude (Opus-class) in a single session, immediately following the Phase 1A Record above. Preceded by confirming and resolving a CI failure on `develop` (run `34075409036`): `packages/core`'s `Test` step failed 4 pre-existing full-replace import tests with `ENOENT ... risk-frameworks.schema.json`, because Phase 1A had added `risk-frameworks`/`risk-controls`/`risk-events` to `V0_1_COLLECTIONS` without adding schema files for them, and `loadImportSchemaValidators` iterates every collection in `V0_1_COLLECTIONS` against the current schema directory. This is exactly the schema bump named in this phase's own scope; no separate fix was needed once D6.1/D6.2 landed.

### Files changed

- **Schema bump (D6.1/D6.2).** `packages/contracts/src/index.ts`: `VERSION_AXES` moved `1.16.0` -> `1.17.0` (schemaVersion/bundleVersion/apiVersion together). New `schemas/explorer-bundle/1.17.0/` directory (copied from `1.16.0/`, every `"const": "1.16.0"` and `$id` path updated to `1.17.0`): `risks.schema.json` gained optional public `primaryCategoryId` and `assessmentState` (enum `assessed|unassessed|legacy|not-comparable`); `links.schema.json` gained optional public `linkRole`; three new collection schemas `risk-frameworks.schema.json`, `risk-controls.schema.json`, `risk-events.schema.json` (envelope-only required properties, since every non-envelope field on these three entities is `sensitive` per D2.8/policy). `scripts/generate-reference-data.mjs`'s `GENERATED_SCHEMA_VERSION` moved to `1.17.0` and `packages/reference-data/src/generated/reference-data.ts` was regenerated (schemaVersion-literal-only diff, confirmed by inspection). `packages/contracts/test-fixtures/standard/bundle.json` axis/entity `schemaVersion` literals bumped to `1.17.0`.
- **Write-rule validation and event derivation.** `packages/core/src/service.ts`: new `validateRiskRules(incoming, existing)` (risk-framework singleton + `validateFramework` reuse; cause/consequence ID uniqueness; `rolls-up-to` graph validated via `validateRollUpEdges` over the full merged graph; `mitigated-by` `application` metadata restricted to that link type and anchor-checked via `validateControlApplicationAnchors`; `risk -> related-to -> risk` `linkRole` restricted to `"secondary-enterprise-association"`; client-supplied `risk-event` entities rejected unless byte-identical to an existing stored event), wired into both `prepareEntitiesForWrite` (direct writes) and `buildImportPlan` (import). `deriveRiskEvents` (Core-derived `reassessed`/`reclassified`/`reparented` events from the stored/incoming diff, appended to the write set in the same transaction as the mutating write, exactly as `appendDueDateHistory` does for Actions) is wired into `prepareEntitiesForWrite` only, not into import — event _derivation_ is scoped to the direct write path per the ADR's literal wording, since deriving events from a sanitised-then-reimported bundle would fabricate spurious `reassessed` events whenever `assessment.assessedAt`/`rationale` are stripped by publication. `assessmentState` is stamped by Core on every risk write and every risk import write via `evaluateRisk`, never trusted from the client. `upsertEntity` (singular) now writes its prepared batch inside a `BEGIN IMMEDIATE`/`COMMIT` transaction so a write that also derives risk-events stays atomic.
- **Explicit migration.** `migrateRiskFramework` (new Core function + `CoreWriteApi`/`CoreService` entry): idempotent — creates the singleton `risk-framework` seeded with `LEGACY_5X5_METHODOLOGY` only when none exists; returns the existing record unchanged on repeat calls (D7.3). No Workshop command wired to it yet (Phase 2).
- **Escalation authoring.** `recordRiskEscalation` (new Core function + `CoreWriteApi`/`CoreService` entry): Core writes an `escalation`-kind `risk-event` directly (bypassing `prepareEntitiesForWrite`'s generic client-write guard, since Core is the author here, not a diff), per D4.1/D4.4. No Workshop command wired to it yet (Phase 2).
- **Import protection (D6.4).** `mergeIncomingEntity` gained a `risk` branch mirroring the existing `action`/`requirement` pattern: `reference`, `description`, `causes`, `consequences`, `ownerTeam`, `reviewBy`, `assessment`, `response`, `externalRefs` are preserved from the existing stored record whenever the incoming (additive-merge) record omits them, so a sanitised-then-reimported publication bundle cannot silently erase them. The full-replace loss-disclosure clause in D6.4 needed no new code: `validateFullReplaceBundleShape` already requires exact current-axis equality, so a full-replace from an older-schema bundle was already structurally impossible before this phase and remains so.
- **Publication preflight (D6.3).** New `runRiskPublicationPreflight(entities)`, run against raw (pre-sanitisation) stored entities before any file is written in both `exportBundle` and `exportTeamShareBundle`; rejects the whole operation naming the offending IDs when any non-deleted Risk has `assessment.basis === "custom" | "unassessed"`, or any `rolls-up-to`/`mitigated-by` link exists.
- **Cold-restore proof (D7.1).** New Core test `"cold restore preserves risk framework, risk fields, and risk-event history (D7.1)"` in `packages/core/src/service.test.ts`, following the existing `scripts/backup-restore-dry-run.mjs` pattern: writes a risk-framework, a Risk with every new sensitive field plus a reassessment, a risk-control, and an escalation event; releases the writer lock; copies `.pspf` into a fresh directory; opens it as a new `createCoreService`; asserts `verifyIntegrity().ok`, exact deep-equality of the Risk and every other new record by ID (scoped to avoid asserting on unrelated baseline reference-data records, whose `updatedAt` the routine reference-data refresh may legitimately touch on open); and proves a fresh writer-lock write succeeds afterwards.
- **New focused tests** (11, all in `packages/core/src/service.test.ts`): `migrateRiskFramework` idempotence; risk-framework singleton + `validateFramework` rejection; `assessmentState` derivation + forged `risk-event` rejection; `reassessed`/`reclassified`/`reparented` event derivation and no-op-emits-nothing; `rolls-up-to` self-link/cardinality/cycle rejection; `mitigated-by` anchor resolution; publication preflight blocking `unassessed` Risks and `rolls-up-to` links (and unblocking again once removed); additive-merge sensitive-field preservation; the D7.1 cold-restore proof.
- **Release-slice wiring**, delegated to and completed by the PSPF Slice Mechanic subagent in the same session: `PSPF_SLICE_VERSION` and every workspace package version moved `1.74.0` -> `1.75.0` (this schema axis bump now has a nameable slice, following the ADR 0097 R1/R2 precedent); `scripts/check-release-candidate.mjs` gained a `[75, "1.17.0"]` table entry, its `>=72`/`>=73`/`>=74` blocks' `"axes should remain 1.16.0"` assertions were scoped to `minorVersion <= 74` so they stop perpetually asserting the old axis, and a new `>=75` block asserts the `1.17.0` schema files and `e2e:v1.75` wiring; `e2e:v1.75`/`e2e:v1.75:run` added to `package.json` and chained from `e2e:v1.74`; `release-gates.json` gained a `"1.75"` block; `scripts/check-adr-coverage.mjs`'s ADR 0098 entry gained `e2e:v1.75` (kept `manual` coverage, since there is still no Workshop/Explorer behaviour); `pspf-acceptance-and-quality-gates.md` gained a v1.75.0 section; `pspf-grand-plan.md`/`pspf-spec-consistency-index.md`/`validation-scenario-1-operator-workflow.md` version stamps updated. No commit, tag, push, or `release:readiness` run.

### Commands and results

- `pnpm --filter pspf-core build && pnpm --filter pspf-core test` — pass, 35/35 (26 pre-existing + 9 new Phase 1B tests, one of which covers 3 event kinds).
- `pnpm --filter @pspf/contracts test` — pass, 169/169 (unchanged from Phase 1A; no new contract fields were needed).
- `pnpm run check:schema-coverage` — pass.
- `pnpm run check:schema-policy` — pass.
- `pnpm run check:explorer-publication` — pass, 27/27 checks (Playwright smoke against the built Explorer app and sample bundles at axis `1.17.0`).
- `pnpm typecheck` — pass.
- `pnpm run lint` — pass (required one `prettier --write` pass on `service.ts`/`service.test.ts` before it did).
- `pnpm build` / `pnpm test` (full monorepo, after the Slice Mechanic's version wiring) — pass.
- `pnpm run check:release-candidate`, `check:gate-integrity`, `check:adr-coverage`, `check:spec-drift` — pass (after the Slice Mechanic's release-slice wiring; all were failing before it purely because of the historical-consistency assertions tied to the axis bump, not because of any Core/contracts defect).

### Known follow-ups for Phase 1C and later

- No Workshop UI or Explorer change exists yet for any of this phase's capabilities; `migrateRiskFramework` and `recordRiskEscalation` are Core-only and unwired to any command, as scoped.
- `createSampleEntities`/home-user sample fixtures (D8.3) still do not include a framework, category template, roll-up chain, shared Action, `risk-control`, or escalation event — deferred to Phase 2 per the original Phase 1A handoff note; Phase 1B's own tests construct minimal fixtures inline instead.
- Event derivation only covers `reassessed`/`reclassified`/`reparented` from a direct-write diff; `reconciled` (crosswalk import, Phase 3B) and `escalation` (operator-authored via `recordRiskEscalation`) are out of this phase's diff mechanism by design, not omissions.
- Score consumers (Explorer `riskBandOf` untouched by design; Workshop tree label, `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES`, Core `highOrExtremeRiskTotal`, Shop supplier attention, contracts consequence chain/suggested actions, Connected View) still multiply `likelihood x impact` directly rather than calling `evaluateRisk`; this is Phase 1C's entire scope.

## Phase 1C Handoff

Phase 1B is complete: Core validation, event derivation, migration, import protection, publication preflight, the schema/axis bump, and the cold-restore proof are all in place and green against their stop gates, including full-workspace `pnpm build`/`pnpm test` and the release-candidate/gate-integrity/adr-coverage/spec-drift gates. Initial task for a fresh Opus session:

> Read docs/risk-overhaul-plan.md § Phase 1C Handoff, the Phase 1B Record immediately above it, adr/0098-workshop-risk-overhaul-contract-baseline.md (§ Consumers to adapt in Phase 1C) and the repository instructions. Execute Phase 1C only: replace every direct `likelihood x impact` computation with a call to `@pspf/contracts`' `evaluateRisk`, guarding against `unassessed`/`not-comparable` states rather than coercing them to zero or the lowest band, across the consumers ADR 0098 names — Workshop tree label (`workshopRiskSeverityLabel`) and `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES` (correct both to invariant E5, per D1.5), Core `highOrExtremeRiskTotal`, Shop supplier attention, contracts consequence chain and suggested actions, and Connected View score. Leave Explorer's `riskBandOf` untouched (it only ever receives legacy-projected published records). For any consumer that needs a single number (strategy priority, suggested-action impact weighting, Shop attention), exclude `unassessed`/`not-comparable` records from the numeric population and disclose the excluded count rather than coercing them in. Provide the fixture proving ADR 0080 strategy-priority ordering is unchanged for existing sample data, or disclose the ordering change explicitly if one occurs. Do not touch Workshop UI beyond the two named severity-label/threshold fixes, Explorer, or Phase 2 workbench UI. Stop after `pnpm --filter @pspf/contracts test`, `pnpm --filter pspf-core test`, `pnpm --filter pspf-workshop test`, `pnpm --filter pspf-shop test`, `pnpm --filter @pspf/connected-view test`, and `pnpm typecheck` all pass, and report the Phase 2 handoff.

Carried forward from Phase 1B, for Phase 1C to consume directly:

- `evaluateRisk(risk, framework)` is the single required entry point; `framework` is optional (consumers without framework access should pass `undefined` and treat `legacy`/`unassessed` states normally — only `custom`-basis assessments need a framework to resolve to `assessed` rather than `not-comparable`).
- Every currently-affected consumer and its exact divergence from invariant E5 is tabulated in ADR 0098 § Context; use that table as the authoritative list of call sites rather than re-discovering them.
- `assessmentState` is already Core-derived and available on every stored Risk; a consumer that only needs the four-state classification (not a band/score) can read it directly instead of calling `evaluateRisk` again.

Stop after: `pnpm --filter @pspf/contracts test`, `pnpm --filter pspf-core test`, `pnpm --filter pspf-workshop test`, `pnpm --filter pspf-shop test`, `pnpm --filter @pspf/connected-view test`, and `pnpm typecheck` all pass, with the ADR 0080 ordering fixture (or its disclosed change) recorded in this document.

## Phase 1C Record

Executed 2026-09-07 with Claude (Opus-class) in a single session, immediately following the Phase 1B Record above.

### Files changed

- **Workshop tree label and severity table (D1.5).** `packages/workshop/src/extension.ts`: `RisksTreeProvider.getChildren` sort and `workshopRiskSeverityLabel` now call `evaluateRisk` instead of `risk.likelihood * risk.impact`; the function takes a `RiskEvaluation` (via `ReturnType<typeof evaluateRisk>`) rather than a `RiskEntity`, returns the E5 band label when present and `"Unassessed"`/`"Not comparable"` otherwise, and the tree sort places risks with no score after every scored risk (title order among themselves) instead of coercing them to the bottom via a `NaN`/`0` score. The `N×N` score suffix is only appended to the detail line when a score exists. `packages/workshop/src/continuous-compliance.ts`: `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES` corrected from its three ad hoc bands (`high >=15`, `medium >=8`, `low >=1`) to the invariant E5 four bands (`extreme >=16`, `high >=10`, `medium >=5`, `low >=1`); `riskSeverityForScore` is unchanged code, only the table moved. `buildStrategyPrioritySummary` now calls `evaluateRisk(risk).score` instead of `risk.likelihood * risk.impact`; a risk whose score is `undefined` (unassessed/not-comparable) is excluded from `topRisks`/`averageAdjustedScore`/`score` and counted in a new `excludedRiskCount` field on `StrategyPrioritySummary`, disclosed in `rationale` (`"N linked risk(s) excluded because no comparable assessment is recorded."`) in every branch, including the zero-linked-risk branch. `highRiskCount` now counts `severityId === "high" || severityId === "extreme"` (previously `"high"` only spanned the whole upper range under the old 3-band table; this preserves that meaning under the corrected 4-band table without changing any existing test expectation, since the only sample score in that band, 20, moves from `"high"` to `"extreme"` and is still counted).
- **Core `highOrExtremeRiskTotal` (Context table row 4).** `packages/core/src/service.ts`'s `buildSnapshotMetrics` resolves the workspace's active `risk-framework` from `collections["risk-frameworks"]` and calls `evaluateRisk(risk, activeFramework)` per open Risk, counting only `band.id === "high" || band.id === "extreme"`; unassessed/not-comparable risks are excluded from the count rather than being coerced into `>= 10` via a raw multiply.
- **Contracts consequence chain / suggested actions (Context table row 6).** `packages/contracts/src/index.ts`'s `enrichActionsWithImpact` — the only site matching the ADR's quoted `(likelihood ?? 0) * (impact ?? 0)`/`>=10`/`>=5` formula — now calls `evaluateRisk(risk)` per linked open risk; a risk with no score is excluded from `riskReduction` and counted in a local `excludedRiskCount`, disclosed as an `impactExplanation` line (`"N linked risk(s) excluded from risk-reduction weighting because no comparable assessment is recorded"`) when non-zero. `risksById`'s map value type changed from a loose `V01Entity & {status?, likelihood?, impact?}` to `RiskEntity` so `evaluateRisk` can be called directly. `buildConsequenceStatement` itself was not touched: it only ever receives an already-computed `maxLinkedRiskSeverity: number`, and its own `>=10`/`>=5` bands already match E5's high/medium thresholds exactly.
- **Shop supplier attention (Context table row 5).** `packages/shop/src/extension.ts`: `deriveCoverageDashboard`'s `supplierRisks` derivation calls `evaluateRisk(risk).score` instead of `risk.likelihood * risk.impact`; `SupplierRisk.score` is now `number | undefined`, never coerced to `0`. A risk is still listed when `status === "open"` even with no comparable score (visibility is preserved), but an undefined score can never satisfy the `>=12` threshold and is excluded from `Math.max` in `deriveContractRisks`'s `maxSupplierRisk` (via a `.filter(isDefined)`) and from the sort key (`?? -1`, so unscored risks sort after every scored one). A new `supplierRiskUnassessedCount` field on `CommercialCoverageDashboard` counts risks whose score could not be evaluated at all (open or not) and is surfaced in the executive-context "Attention required" paragraph when non-zero. The interactive supplier-risk HTML table renders `item.score ?? "Unassessed"`; the existing CSV/HTML export helpers already render `undefined` as an empty cell via `value ?? ""`, so no change was needed there. `buildSupplierVerdict` (a second, separate Shop consumer of the same defect, not itself named in the ADR row but sharing the identical formula in the same file) was also corrected: `highestRiskScore` is now `number | undefined`, computed via `evaluateRisk` over only the open linked risks that have a score, and the verdict statement says `"(severity not comparable)"` instead of interpolating an undefined/coerced number when no open linked risk has one.
- **Connected View score (Context table row 7).** `packages/connected-view/src/index.ts`'s `riskBadge` calls `evaluateRisk(risk)` instead of clamping `likelihood`/`impact` to 1..5 and multiplying; a risk with no band renders a distinct neutral `"Unassessed"`/`"Not comparable"` badge instead of being coerced into the `"Low"` tone. Band-to-tone mapping is `extreme`/`high` → `danger`, `medium` → `warn`, `low` → `info`, preserving the existing three-tone visual language.
- **Explorer `riskBandOf`** (`packages/explorer/src/domain/analytics.ts`) and every other Explorer call site: untouched, as directed — Explorer only ever receives legacy-projected published records, so the existing raw multiply there already matches E5 exactly (verified by reading the file; no edit made).
- **Other Workshop UI likelihood/impact call sites** (e.g. the `>=16` `highRisks` count and the `maxLinkedRiskSeverity` reducers feeding `buildConsequenceStatement` in `extension.ts`, `pentest-workbench.ts`, `requirement-card-view.ts`) and **`brief-renderer`**: untouched, per the explicit "do not touch Workshop UI beyond the two named severity-label/threshold fixes" instruction. These remain raw-multiply and are not part of this phase's named consumer list.
- **Tests.** `packages/workshop/src/continuous-compliance.test.ts`: renamed and corrected the severity-band test to invariant E5 (`riskSeverityForScore(20).id === "extreme"`, etc., replacing the old 3-band assertions); added a test proving `buildStrategyPrioritySummary` excludes an `unassessed`-basis risk from `topRisks`/scoring and discloses `excludedRiskCount`/rationale text; added the ADR 0080 ordering fixture (below). No other existing test needed a behaviour-changing edit — `highRiskCount`'s broadened filter and the unchanged `evaluateRisk` score for legacy risks meant every prior assertion (band thresholds, dedup, unresolved-reference handling) still passes unmodified.

### ADR 0080 ordering fixture

Added `"ADR 0080: strategy-priority ordering is unchanged for existing sample data (Phase 1C fixture)"` to `continuous-compliance.test.ts`. It builds `buildSampleWorkspaceEntities()`, asserts every sample Risk still has no `assessment` field (so `evaluateRisk` reads it as legacy basis) and that `evaluateRisk(risk).score === risk.likelihood * risk.impact` for every one — i.e. the new formula is bit-identical to the raw multiply the pre-1C code used for all real sample data — then runs every sample `StrategyEntity` choice through `buildStrategyPrioritySummary` and asserts `excludedRiskCount === 0` throughout. Since the score values feeding the sort/band computation are unchanged and nothing is excluded, strategy-priority ordering for existing sample data is unchanged; no ordering-change disclosure is needed.

### Commands and results

- `pnpm --filter @pspf/contracts build` then `pnpm --filter @pspf/contracts test` — pass, 169/169 (unchanged count; no new contract fields, only a behaviour fix inside `enrichActionsWithImpact` and a new value import of `evaluateRisk`).
- `pnpm --filter pspf-core build` then `pnpm --filter pspf-core test` — pass, 35/35 (unchanged from Phase 1B; `highOrExtremeRiskTotal`'s new evaluator-based computation is not separately unit-tested in this phase beyond the existing snapshot-metrics tests it already had to keep passing).
- `pnpm --filter pspf-workshop build` then `pnpm --filter pspf-workshop test` — pass, 110/110, including the new/updated `continuous-compliance.test.ts` cases.
- `pnpm --filter pspf-shop build` then `pnpm --filter pspf-shop test` — pass, 19/19, including the existing supplier-verdict and contract-risk-attention tests unmodified.
- `pnpm --filter @pspf/connected-view build` then `pnpm --filter @pspf/connected-view test` — pass, 8/8 (no existing test asserted `riskBadge`'s exact label/tone, so none needed updating).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).

Not run in this phase (outside its stop-gate list): `pnpm lint`, full `pnpm build`/`pnpm test`, `release:readiness`, and the schema/gate/ADR-coverage checks — none of this phase's edits touch schemas, versions, or gates.

### Known follow-ups for Phase 2 and later

- `buildSupplierVerdict` and the interactive Shop supplier-risk table were corrected alongside the ADR-named "Shop supplier attention" dashboard because they share the exact same file and defect, but they are a disclosed addition beyond the ADR's literal consumer list, not a scope expansion into Shop UI redesign — no new panel, command, or field beyond the one disclosure count was added.
- The Workshop `>=16`/`>=10` raw-multiply sites left untouched (exposure summary's `material-risks` component, the posture/strategy summary reducers, `pentest-workbench.ts`, `requirement-card-view.ts`) and `brief-renderer`'s two raw-multiply sites remain defects relative to E5/D1.3 in spirit, but are explicitly out of this phase's named scope; a future phase should decide whether to fold them into the same `evaluateRisk` treatment or leave them as legacy-only display code pending Phase 2's editor work.
- No Workshop UI, Explorer, or Phase 2 workbench surface was touched, per the explicit exclusion in this phase's task.

## Phase 2 Handoff

Phase 1C is complete: every named score consumer calls `evaluateRisk`, guards `unassessed`/`not-comparable` states without coercion, discloses excluded counts where a single number is needed, and the ADR 0080 ordering fixture proves existing sample-data ordering is unchanged. All five named package test suites and `pnpm typecheck` pass. Initial task for a fresh Sonnet session (escalate contract gaps to Opus per the model-assignment table):

> Read docs/risk-overhaul-plan.md § Phase 2 Handoff, the Phase 1C Record immediately above it, § Workbench and Presentation, § Proposed Data Design, § Current Implementation Anchors, adr/0098-workshop-risk-overhaul-contract-baseline.md and the repository instructions. Execute Phase 2 (Record and assess) only: build the structured Risk editor, register, and risk-framework configuration surface inside the existing Item Detail panel and `pspf.workshop.openRisksList`/`openEntityEditor` commands, replacing the current title/status/likelihood/impact-only `renderRiskEditor`, plus the Hierarchy and Matrix presentation views described in § Workbench and Presentation. Wire the already-built Phase 1B Core functions (`migrateRiskFramework`, `recordRiskEscalation`) and Phase 1A/1C contracts (`evaluateRisk`, `resolveAppetite`, `validateFramework`, `detectRollUpCycle`, `RiskAssessment`, `RiskFrameworkEntity`) into real Workshop commands and webview panels for the first time. Do not increase the Essentials surface budget (`scripts/lib/essentials-surface-baseline.json`); reuse `editorShell`/`recordWorkbenchShell`/the Item Detail host rather than adding a new top-level route or panel. Do not touch Explorer, Treatments/controls/bow-tie (Phase 3A), source crosswalk (Phase 3B), or presentation outputs/cards (Phase 4A). Update `createSampleEntities`/home-user sample fixtures (D8.3) with at least one framework, category template, and roll-up chain if needed to exercise the new editor. Stop after behavioural, keyboard, and visual checks for the new editor/register/hierarchy/matrix pass, `pnpm --filter pspf-workshop test` and `pnpm typecheck` pass, `pnpm run check:essentials-surface` and `pnpm run check:ux-coverage` still pass, and report the Phase 3A handoff.

Carried forward from Phase 1A/1B/1C, for Phase 2 to consume directly rather than reinvent:

- `evaluateRisk`, `resolveAppetite`, `validateFramework`, `detectRollUpCycle`, `validateRollUpEdges`, `validateControlApplicationAnchors`, `LEGACY_5X5_METHODOLOGY` (`packages/contracts/src/risk-model.ts`) are the full pure-model surface; Phase 2 wires them into UI rather than adding new pure logic unless a genuine gap is found.
- `migrateRiskFramework`/`recordRiskEscalation` (`packages/core/src/service.ts`) exist but are unwired to any command; Phase 2 is the first phase expected to call them from Workshop.
- Every score-consumer defect named in ADR 0098 § Context is now fixed except the explicitly-deferred Workshop/`brief-renderer` sites listed in the Phase 1C Record's Known follow-ups; Phase 2's new editor must use `evaluateRisk`/`assessmentState` from the outset rather than reintroducing a raw multiply.
- `RisksTreeProvider`/`workshopRiskSeverityLabel` already render the corrected E5 label and an explicit `"Unassessed"`/`"Not comparable"` state; Phase 2's register/editor should reuse the same vocabulary rather than inventing new severity wording.

Stop after: the behavioural/keyboard/visual checks named in § Implementation and Verification Map items 1-2 and 6 for the new editor surfaces, `pnpm --filter pspf-workshop test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, and `pnpm run check:ux-coverage` all pass, with the sample-fixture additions (if any) and the Phase 3A handoff recorded in this document.

## Phase 2 Record

Executed 2026-09-07 with Claude (Sonnet-class) in a single session, immediately following the Phase 1C Record above.

### Files changed

- `packages/workshop/src/risk-workbench.ts` (new): pure, deterministic view-model builders reused by every new surface — `getActiveRiskFramework`, `riskRollUpEdgesFromLinks`/`findPrimaryParentId`/`findRollUpLink`, `candidateParentRisks` (cycle-safe primary-parent picker), `riskCategoryOptions`/`riskCategoryLabel`/`riskCategoryPath`, `parseCategoryOutline`/`renderCategoryOutline` (stable-ID-preserving textarea outline editor for categories, archiving removed nodes per D2.2 rather than deleting them), `riskBandLabel`/`riskAppetiteLabel`, `buildRiskRegisterRows`, `buildRiskHierarchyForest` (cycle-tolerant), `buildRiskMatrixModel` (generic over any `RiskMethodologyRevision`, not hardcoded to legacy).
- `packages/workshop/src/risk-workbench.test.ts` (new, 9 tests): category flattening/archiving/outline round-trip, cycle-safe parent candidates, register row disclosure, hierarchy forest nesting with a tolerated cycle, matrix cell counts and unassessed/not-comparable disclosure.
- `packages/workshop/src/extension.ts`: replaced the title/status/likelihood/impact-only `renderRiskEditor` with `renderRiskWorkbench`, a `riskView`-dispatching workbench (`record | register | hierarchy | matrix | framework`) reusing `editorShell`/`recordWorkbenchShell`/the existing Item Detail panel — no new `createWebviewPanel` call. `openRisksList` now opens the workbench directly in Register state instead of a native quick pick (ADR 0098 D8.1), falling back to the existing "no risks yet" information message when the register is genuinely empty (see disclosed cut below). The Record view gained Reference/Description/Owner team/Review by/Primary category/Response/Causes/Consequences fields, an Assessment section (basis select; legacy scores unchanged; a generic custom-methodology picker driven by whatever methodologies exist in the active `risk-framework`, expressed as combined likelihood×impact→band options rather than two cross-dependent selects), a Relationships section (primary-parent `rolls-up-to` picker via `candidateParentRisks`, read-only secondary `related-to` associations, direct child count), and a History section (Core-derived `risk-event` table plus a "Record escalation" mini-form calling the new `pspf.core.recordRiskEscalation` command). New internal webview messages — `setRiskWorkbenchView`, `migrateRiskFramework`, `setRiskParent`, `recordRiskEscalation`, `saveRiskFrameworkCategories`, `saveRiskFrameworkAppetite` — are handled inside the existing `openEntityEditor` message closure (no new `vscode.commands.registerCommand`/package.json contribution, so the Essentials command budget is untouched). `buildUpdatedEntity`'s `risk` case now saves every new field and derives `RiskAssessment` from the form. `recordWorkbenchMeta`/`compareWorkbenchRecords`/`RisksTreeProvider`-adjacent sort now call `evaluateRisk` instead of a raw `likelihood * impact` (closing the one remaining raw-multiply site the Phase 1C Record flagged as a future follow-up, not a new defect).
- `packages/workshop/src/webview/shell.ts`: client script gained handling for the six new commands above (`saveCommands` exemption set so Framework/escalation saves are not blocked behind the dirty-record-navigation prompt; `pendingRiskView` carried through `confirmDirtyNavigation` so switching views while a record is dirty still prompts correctly; the generic `<select data-command>` change handler now also forwards `data-entity-id` for the parent-risk picker).
- `packages/workshop/src/workshop-ui.test.ts`: updated the existing "Workshop edit buttons open list pickers before edit panels" regression test to encode Risk's deliberate ADR 0098 D8.1 exception (register-first, not quick-pick-first) instead of asserting the old `pickEntityForEdit(...)"Edit Risk"` pattern.
- `packages/core/src/extension.ts`: exposed `migrateRiskFramework` and `recordRiskEscalation` (already built in Phase 1B) as internal `pspf.core.migrateRiskFramework`/`pspf.core.recordRiskEscalation` commands, following the existing `upsertEntity`/`listEntities` pattern — no package.json command contribution, so they do not count against any command budget.
- `packages/contracts/src/index.ts` (D8.3, scoped down per this phase's task wording to framework + category template + roll-up chain only): both `buildSampleWorkspaceEntities` (enterprise) and `buildHomeSampleWorkspaceEntities` (home) gained a seeded `risk-framework` sample entity with a three-level category template (Enterprise › Digital › Technology, plus a sibling Cyber category; Household › Devices/Online safety for home) and the Legacy 5x5 methodology; `riskEncryption`/`riskUnpatched` gained a `primaryCategoryId`; the enterprise sample gained one `rolls-up-to` link (`riskAccess → riskGovernance`) to exercise the Hierarchy view. No new contract fields, schema, or version-axis change was needed — every field used already existed from Phase 1A/1B.
- `pspf-entity-ux-coverage.json`: refreshed the `risk` entry's `standard`/`gap`/`nextSlice` for the new workbench, and added `risk-framework` (`user-editable`, category/appetite editing only), `risk-control` (`generated-readonly`, explicitly "not yet built", deferred to Phase 3A), and `risk-event` (`generated-readonly`, history read plus operator-authored escalation only) so `check:ux-coverage`'s exhaustive contract-entity-type check passes.

### Deliberate scope cuts, disclosed

- **Register empty state.** `openRisksList` still needs a concrete `RiskEntity` to open the shared `pspfEntityDetail` panel (the whole Item Detail host is built around "one current entity"), so a truly empty workspace still shows the pre-existing native "No Risk records found" information message rather than an in-panel empty-state register. Restructuring `openEntityEditor` to open with no entity would touch all seven editable entity types and was judged out of proportion for this phase; flagged as a candidate follow-up rather than done silently.
- **Custom methodology/matrix authoring.** The Framework view supports the category outline and appetite rules against whatever methodologies already exist in the `risk-framework` record, but there is no UI to author a _new_ methodology's levels/cells/bands — only the seeded Legacy 5x5 exists in practice. The Assessment section's "Custom methodology" basis is therefore currently equivalent in practice to Legacy 5x5 (just resolved through the framework-lookup path instead of the direct formula), which is enough to prove the generic custom-assessment plumbing works end-to-end without inventing a matrix-builder UI this phase.
- **Secondary `related-to` associations** are read-only in the Relationships section; no authoring UI was added (D3.2 is not explicitly named in this task's exclusion list, but is closer in spirit to Phase 3A's relationship work than to the named "record and assess" scope).
- **Home sample** gained its own framework/category template but not a second `rolls-up-to` chain, since one already exists in the enterprise sample and the task's wording ("roll-up chain if needed") was read as licensing this.
- **No live VS Code behavioural/keyboard/visual walkthrough was run** in this session (no Extension Development Host available in this environment). Verification instead rests on: full `pnpm build`/`pnpm test`/`pnpm typecheck`/`pnpm run lint` across the whole monorepo; 9 new focused `risk-workbench.test.ts` unit tests; the full existing `pspf-workshop` (119/119) and `@pspf/contracts` (169/169) suites passing unchanged or updated; and a manual reading confirming every new interactive control is a native, inherently keyboard-operable element (`<select>`, `<button>`, `<textarea>`, `<details>/<summary>`, `<table>`) and that every dynamic string passes through `escapeHtml`/`metricCardHtml`/`selectField`/`recordTable`'s existing escaping. § Implementation and Verification Map item 6 (empty/typical/500-risk fixtures, 320/768/1440px, dark/light/high-contrast, zero axe findings) is **not** covered by this and should be run in a live VS Code session before this phase is treated as fully closed.

### Commands and results

- `pnpm --filter @pspf/contracts build` then `pnpm --filter @pspf/contracts test` — pass, 169/169 (unchanged count; only sample-fixture data changed, no new contract fields/tests needed).
- `pnpm --filter pspf-workshop build` then `pnpm --filter pspf-workshop test` — pass, 119/119 (110 pre-existing + 9 new `risk-workbench.test.ts`; one pre-existing regression test updated for the deliberate D8.1 Risk exception, see Files changed).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).
- `pnpm run check:essentials-surface` — pass, unchanged: commands 72/72, panels 30/30.
- `pnpm run check:ux-coverage` — pass, 27 contract entity types covered (was 24; `risk-framework`/`risk-control`/`risk-event` added).
- `pnpm build` (full monorepo, including Explorer's sample-bundle regeneration against the updated sample fixtures) — pass.
- `pnpm test` (full monorepo) — pass, including `pspf-core`'s existing 35/35 (untouched by this phase; the two new `pspf.core.*` commands wrap already-tested Phase 1B functions and were not re-tested at the command layer).
- `pnpm run lint` (AU-English, ESLint, Prettier) — pass (needed one `prettier --write` pass on the four touched/new files before it did).
- `pnpm run check:schema-policy`, `pnpm run check:risk-source-integration` — pass (run as a sanity check; neither publication policy nor 6clicks integration was touched this phase).

### Known follow-ups for Phase 3A and later

- Live VS Code behavioural/keyboard/visual/accessibility verification (see disclosure above) has not been run against the new Register/Record/Hierarchy/Matrix/Framework surfaces.
- The Relationships section shows secondary `related-to` associations read-only; Phase 3A's shared Action/`risk-control` linking work is a natural place to add authoring for these too, if in scope.
- Framework configuration is limited to categories and appetite rules against existing methodologies; no methodology/matrix authoring UI exists.
- The Register's search/filter is a single free-text box (native `data-filter-target` mechanism reused from the Requirement browser); the storyboard's fuller "category/source/team/status/appetite/review filters, grouping/sort, resizable columns and density" are not built.
- Bow-tie, coverage map, executive cards, and CSV/TSV crosswalk import remain entirely out of scope, as directed (Phase 3A/3B/4A).

## Phase 3A Handoff

Phase 2 is complete: the Risk workbench (Register/Record/Hierarchy/Matrix/Framework) is wired into the existing Item Detail panel and `openRisksList`/`openEntityEditor`, `migrateRiskFramework`/`recordRiskEscalation` are callable from Workshop for the first time, and the named stop-gate checks pass (see the Phase 2 Record above for the live-verification disclosure). Initial task for a fresh session (Sonnet lead, Opus review per the model-assignment table):

> Read docs/risk-overhaul-plan.md § Phase 3A Handoff, the Phase 2 Record immediately above it, § Workbench and Presentation, § Proposed Data Design (Treatments, Controls and Bow-Tie; History and Escalation), adr/0098-workshop-risk-overhaul-contract-baseline.md and the repository instructions. Execute Phase 3A (Treat, explain, escalate) only: add `risk-control` register/authoring, wire `risk -> treated-by -> action` (link/reuse/create Actions from the Risk record, affected-risk preview) and `risk -> mitigated-by -> risk-control` (typed `LinkEntity.application` metadata per D3.5) into the Risk record's Relationships/Treatments section, add the bow-tie tab (causes/preventive controls/event/recovery controls/consequences, anchor validation via `validateControlApplicationAnchors`) and the coverage map view named in § Workbench and Presentation, and complete secondary `risk -> related-to -> risk` authoring if not already sufficient. Do not increase the Essentials surface budget; reuse the existing Item Detail host and the `riskView` toolbar pattern established in Phase 2 (add `treatments`/`bowtie`/`coverage` view modes alongside `register`/`hierarchy`/`matrix`/`framework` rather than a new panel). Do not touch source crosswalk (Phase 3B) or presentation outputs/cards (Phase 4A). Update sample fixtures with a `risk-control`, at least one `mitigated-by` application, and a shared Action treating two Risks if needed to exercise the new UI. Verify event integrity (no forged/rewritten events) and coverage-count aggregation (distinct IDs, direct vs descendant, never summed/averaged bands) as an Opus review pass. Run a live VS Code behavioural/keyboard/visual/accessibility check this time (§ Implementation and Verification Map item 6 was not completed in Phase 2) alongside `pnpm --filter pspf-workshop test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, and `pnpm run check:ux-coverage`, and report the Phase 3B handoff.

Carried forward from Phase 2, for Phase 3A to consume directly rather than reinvent:

- `packages/workshop/src/risk-workbench.ts`'s pure builders (`getActiveRiskFramework`, category/appetite/hierarchy/matrix helpers) and the `riskView`-dispatching `renderRiskWorkbench`/`riskWorkbenchToolbar` pattern in `extension.ts` are the established place to add `treatments`/`bowtie`/`coverage` view modes.
- The internal webview-message pattern (new `data-command` values handled inside `openEntityEditor`'s message closure or `wireWorkshopPanelMessages`, wired into `webview/shell.ts`'s client script) is how every Phase 2 mutation avoided growing the Essentials command/panel budget; Phase 3A's treatment/control/bow-tie authoring should follow the same pattern.
- `validateControlApplicationAnchors`, `RiskControlApplication`, `RiskControlEntity` (`packages/contracts/src/risk-model.ts`) and the five `OPERATOR_LINK_RULES` rows from Phase 1A (`workshop-risk-treated-by-action`, `workshop-risk-mitigated-by-risk-control`, `workshop-action-addressed-by-risk-control`, plus the roll-up/secondary-association rows already used by Phase 2) are typed and policy-declared; Core write-rule validation for `mitigated-by` anchors already exists from Phase 1B.
- `riskCategoryPath(framework, risk.primaryCategoryId)` and `riskAppetiteLabel(resolveAppetite(...))` are the established vocabulary for category/appetite display; reuse rather than reinvent wording.

Stop after: a live VS Code behavioural/keyboard/visual/accessibility check for the new Treatments/bow-tie/coverage surfaces (empty/typical/large fixtures, keyboard equivalents, zero serious/critical axe findings), `pnpm --filter pspf-workshop test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, and `pnpm run check:ux-coverage` all pass, with the Phase 3B handoff recorded in this document.

## Phase 3A Record

Executed 2026-09-07 with Claude (Sonnet-class) in a single session, immediately following the Phase 2 Record above. Implementation and the requested event-integrity/aggregation review were both performed in this one session; see the disclosed limitation below rather than an independent Opus reviewer pass.

### Files changed

- `packages/workshop/src/risk-workbench.ts`: new pure builders — `buildRiskTreatmentActions` (`risk -> treated-by -> action` rows with the D3.7 shared-Action "affected-risk preview", resolved via the full risk list rather than IDs alone), `candidateActionsForTreatment` (excludes already-linked and `done`/`cancelled` Actions), `buildRiskControlApplications` (`risk -> mitigated-by -> risk-control` rows, re-running `validateControlApplicationAnchors` client-side to flag a stale anchor even though Core already rejects one at write time), `candidateControlsForMitigation` (excludes already-linked and `retired` controls), `buildRiskBowTieModel` (groups controls by role under their anchored cause/consequence, discloses `unresolvedApplicationCount`), `descendantRiskIds` (cycle-tolerant `rolls-up-to` descendant walk), and `buildRiskCoverageRows` (D3.8: distinct action/control ID sets per risk, direct vs descendant, descendant explicitly excludes anything already counted as direct so a shared Action is never double-counted; `bandLabel` is a per-risk string, never summed or averaged across risks).
- `packages/workshop/src/risk-workbench.test.ts`: 8 new focused tests covering the shared-Action preview, candidate filtering (both directions), stale-anchor detection, bow-tie grouping and a "visible gap" case, cycle-tolerant descendant walking, and the direct/descendant no-double-count coverage case.
- `packages/workshop/src/extension.ts`: `RiskWorkbenchView` gained `"treatments" | "bowtie" | "coverage"`; `riskWorkbenchToolbar` gained matching buttons. `renderRiskWorkbench` dispatches Treatments/Bow-tie through `recordWorkbenchShell` (selected-risk detail views, per the storyboard's "selected-risk view within detail, not a standalone dashboard") and Coverage as a workspace-wide full-width view alongside Register/Hierarchy/Matrix/Framework. New render functions `renderRiskTreatmentsContent` (treating-Actions table plus a link/create mini-form; mitigating-controls table plus a link-with-role/effectiveness/anchors mini-form and a "Create risk control" native-prompt flow; a workspace-wide "All risk controls" register table underneath, satisfying the "risk-control register/authoring" requirement without a new toolbar view), `renderRiskBowTieContent` (causes/preventive-controls and consequences/recovery-controls as two semantic tables, a visible-gap count, and an unresolved-application-anchor count), `renderRiskCoverageContent` (a filterable semantic table of distinct direct/descendant action and control counts per risk, reusing the existing generic `data-filter-target` client mechanism). `riskRelationshipsSection` gained a "Link secondary risk" button, completing D3.2 authoring (previously read-only). New mutation functions: `linkExistingActionToRiskFromFields`, `createActionForRisk`, `createRiskControl`, `mitigateRiskWithControlFromFields` (validates anchors client-side before writing, mirroring Core's own check), `linkSecondaryRiskAssociation` (cycle-safe candidate list excluding already-associated risks). All wired as new `data-command` values inside the existing `openEntityEditor` message closure — no new `vscode.commands.registerCommand`, `createWebviewPanel`, or package.json contribution.
- `packages/workshop/src/webview/shell.ts`: the five new commands added to the `saveCommands` dirty-check exemption set (defensive; none of the new views render the tracked `form.form-grid`, so the dirty-check was never actually triggered by them) and split across the existing entity-id-based and fields-reading generic dispatch blocks, following the exact pattern established for `linkEvidenceToRequirements`/`recordRiskEscalation` in Phase 2.
- `packages/contracts/src/index.ts` (enterprise sample only, per the Phase 2 precedent of scoping D8.3 fixtures down to what a phase actually exercises): `riskEncryption` gained one `cause`/one `consequence`; a new `risk-control` sample (`riskControlEncryption`, "Full-disk encryption enforced by device policy", `state: "active"`); a `mitigated-by` link from `riskEncryption` to it with a `preventive`, `partially-effective` application anchored to the new cause (exercises Treatments, Bow-tie's preventive column, and Bow-tie's visible-gap disclosure for the still-unmitigated consequence); three canonical `treated-by` links — `riskEncryption -> actionEncryption` and, reusing the same Action, `riskGovernance -> actionEncryption` (the shared-Action fixture, deliberately linking two risks with no hierarchy relationship so the affected-risk preview is exercised independently of coverage's direct/descendant distinction), plus `riskAccess -> actionAccess` (`riskAccess` already rolls up to `riskGovernance` from Phase 2, so this also gives Coverage a genuine direct-on-child/descendant-on-parent case with no overlap against `riskGovernance`'s own direct Action).
- `pspf-entity-ux-coverage.json`: `risk`'s `relationships` moved to `complete` and its `standard`/`gap`/`nextSlice` updated to name Phase 3A's surfaces and point to Phase 3B. `risk-control` moved from `generated-readonly`/all-`missing` to `user-editable` with `list`/`create`/`relationships` `complete`; `detail` stays `partial` and `edit`/`delete` stay `missing` (disclosed below).

### Deliberate scope cuts, disclosed

- **No coverage diagram.** § Workbench and Presentation calls for "a focused many-to-many risks/actions/controls diagram... and an equivalent semantic table"; only the semantic table was built. Workshop's webview host has no existing 2D graph-rendering dependency wired into its build (unlike Explorer's `@pspf/connected-view` usage), and introducing one for a single view was judged disproportionate against the "lazy-loaded 2D only" guidance and this phase's own scope. The table is fully equivalent in content (distinct direct/descendant action and control counts and titles per risk, filterable, with a link back into each risk's own record) and is explicitly labelled in the UI as a placeholder for the diagram.
- **Risk control has no edit or retire UI.** Controls are created via a native input-box/quick-pick sequence (title, definition, owner team, state) and then linked; there is no flow to change a control's fields or state afterwards from Workshop. This mirrors the same kind of first-pass gap Phase 2 disclosed for custom methodology authoring, and is recorded as the `risk-control` UX-coverage `nextSlice`.
- **No unlink/remove UI.** Treatments, control applications, and secondary associations can be added but not removed from the Workshop UI in this phase; the task's own wording ("link/reuse/create Actions... affected-risk preview") did not name removal, and adding it was judged a scope expansion rather than a necessary part of "treat, explain, escalate."
- **Bow-tie is table-only, not a graphical diagram**, for the same accessibility/no-new-dependency reasoning as Coverage; the storyboard's own wording for Bow-tie ("Selected-risk view within detail, not a standalone dashboard") is satisfied by presenting it as a `recordWorkbenchShell`-hosted detail view rather than a workspace-wide surface.
- **No live VS Code Extension Development Host walkthrough was run.** This session's tool environment has no interactive VS Code GUI or Extension Development Host available (the same constraint Phase 2 disclosed). As a substitute, every new interactive control was manually verified to be a native, inherently keyboard-operable HTML element (`<button>`, `<select>`, `<input>` including `type="search"`/`type="checkbox"`/`type="hidden"`, `<textarea>`, `<fieldset>/<legend>`); every dynamic string was confirmed to route through `escapeHtml` or `recordTable`'s own per-field escaping (explicitly checked against the known `tableCell` raw-HTML exception for fields literally named `action`/`trend`, which none of this phase's new field names collide with); the new Coverage filter input was confirmed to reuse the exact same generic `data-filter-target`/`data-search` client-side mechanism already wired for Register, rather than inventing a new one; and no colour-only signal was introduced (Coverage's "Covered"/"Uncovered" and Treatments' "Resolved"/"N unresolved" are text, not colour). This is a static-review substitute, not a live behavioural/keyboard/visual/axe-accessibility pass, and should be run in a real VS Code session before this phase is treated as fully closed — the same follow-up Phase 2 left open.

### Event integrity and coverage-aggregation review (in-session, not an independent Opus session)

- **Event integrity.** No file under `packages/core` was touched this phase. Every new mutation (`linkExistingActionToRiskFromFields`, `createActionForRisk`, `createRiskControl`, `mitigateRiskWithControlFromFields`, `linkSecondaryRiskAssociation`) writes only `link`/`action`/`risk-control` entities through the existing `pspf.core.upsertEntity(ies)` commands, which flow through Phase 1B's unchanged `prepareEntitiesForWrite` → `validateRiskRules` → `deriveRiskEvents` pipeline. `deriveRiskEvents` only derives from a Risk's own diff (assessment/category/roll-up-parent changes); none of this phase's writes touch those fields, so no new Risk write diff exists to derive an event from, and no code path in this phase constructs, edits, or deletes a `risk-event` entity. Core's existing rejection of any client-supplied `risk-event` that is not byte-identical to a stored one is unchanged and untouched.
- **Coverage aggregation.** `buildRiskCoverageRows` builds `directActionIdsByRisk`/`directControlIdsByRisk` as `Set<string>` keyed by risk ID (so repeated links to the same Action/Control never inflate a count), then for each risk unions its descendants' direct sets while explicitly filtering out anything already present in that risk's own direct set before adding to the descendant set — verified by the "without double-counting" unit test using a shared Action linked to both a parent and its child. No step anywhere sums or averages `bandLabel`/`evaluateRisk` output; each risk's band is computed and displayed independently. `descendantRiskIds` walks a `childrenOf` adjacency map with a `visited` guard, so a cyclic (defensively tolerated, per Phase 2's `buildRiskHierarchyForest` precedent) `rolls-up-to` graph cannot infinite-loop or double-visit a descendant.

### Commands and results

- `pnpm --filter pspf-workshop build` then `pnpm --filter pspf-workshop test` — pass, 127/127 (119 pre-existing + 8 new `risk-workbench.test.ts` cases).
- `pnpm --filter @pspf/contracts build` then `pnpm --filter @pspf/contracts test` — pass, 169/169 (unchanged count; only sample-fixture data changed).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).
- `pnpm run check:essentials-surface` — pass, unchanged: commands 72/72, panels 30/30.
- `pnpm run check:ux-coverage` — pass, still 27 contract entity types covered.
- `pnpm build` (full monorepo, including Explorer's sample-bundle regeneration against the updated sample fixtures) — pass.
- `pnpm test` (full monorepo) — pass: `webview-shell` 29/29, `@pspf/contracts` 169/169, `assurance` 12/12, `shop` 19/19, `pub` 49/49, `@pspf/connected-view` 8/8, `brief-renderer` 44/44, `pspf-workshop` 127/127, `pspf-core` 35/35 (unchanged; no Core file was touched this phase).
- `pnpm run lint` (AU-English, ESLint, Prettier) — pass (needed one `prettier --write` pass on the three touched/new files before it did).
- `pnpm run check:schema-policy` — pass (sanity check; no publication policy or schema field was touched this phase).

### Known follow-ups for Phase 3B and later

- Live VS Code behavioural/keyboard/visual/accessibility verification (see disclosure above) has still not been run against any Risk workbench surface across Phase 2 or Phase 3A.
- No coverage or bow-tie graphical diagram exists; both are semantic tables only.
- `risk-control` has no edit/retire flow; a control's fields are fixed at creation.
- No unlink/remove authoring exists for treatments, control applications, or secondary associations.
- Source register crosswalk (external references, CSV/TSV import, reconciliation) remains entirely out of scope, as directed (Phase 3B).

## Phase 3B Handoff

Phase 3A is complete: `risk-control` register/authoring, `treated-by`/`mitigated-by` linking with typed application metadata, the bow-tie and coverage views, and secondary `related-to` authoring are all wired into the existing Item Detail panel and `riskView` toolbar, and the named stop-gate checks pass (see the Phase 3A Record above for the live-verification disclosure). Initial task for a fresh session (Sonnet lead for UI/parser orchestration, Opus lead for mutation semantics/review per the model-assignment table):

> Read docs/risk-overhaul-plan.md § Phase 3B Handoff, the Phase 3A Record immediately above it, § Proposed Data Design (External Registers), adr/0098-workshop-risk-overhaul-contract-baseline.md (D5.4, D6.4) and the repository instructions. Execute Phase 3B (source crosswalk) only: add manual `externalRefs[]` authoring to the Risk record (source register, external ID, original external rating as text, source dates, safe `https:`-only reference URL validated before write, reconciliation timestamp), and a previewed CSV/TSV import flow (a suitable maintained parser, a field template, column mapping, source-register selection, and a create/reuse/update/conflict/unmatched preview requiring explicit confirmation before any write). Match by stable external identity (source register plus external ID); title similarity may only suggest a possible duplicate, never merge automatically. Stage unresolved crosswalk rows outside the live graph; reject cycles/dangling references in the proposed final graph before commit. Revalidate against the current store immediately before commit, commit atomically with a recovery checkpoint, and prove repeat imports are idempotent (a second import of the same file changes nothing). Preserve local notes/relationships for rows absent from a later import; never infer deletion from an absent source row. Do not change authentication or add a network dependency; do not touch the 6clicks integration, Bow-tie/Coverage/Treatments views (Phase 3A), or presentation outputs/cards (Phase 4A). Update sample fixtures with a crosswalk-eligible Risk and a small fixture CSV/TSV file used by the new tests if needed. Stop after `pnpm --filter pspf-workshop test`, `pnpm --filter pspf-core test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, and `pnpm run check:ux-coverage` all pass, plus an idempotent-reimport test and a rollback/atomicity test are recorded with evidence in this document, and report the Phase 4A handoff.

Carried forward from Phase 3A/1A, for Phase 3B to consume directly rather than reinvent:

- `RiskExternalRef` (`packages/contracts/src/risk-model.ts`, D5.4) already types `sourceRegisterId`, `externalId`, `externalRating`, `sourceUpdatedAt`, `referenceUrl?`, `reconciledAt`; `RiskEntity.externalRefs` and its `sensitive` publication policy already exist from Phase 1A. Phase 3B adds authoring UI and import plumbing, not new contract fields, unless a genuine gap is found.
- `RiskFrameworkEntity.sourceRegisters` (D2.1) is already typed (`{ id, label }[]`) but unauthored; Phase 3B is the natural place to add a source-register picker/authoring alongside `externalRefs`.
- The `risk-event` `kind: "reconciled"` value already exists in the `RiskEventKind` union (Phase 1A) but has no Core derivation path yet (Phase 1B's `deriveRiskEvents` only covers `reassessed`/`reclassified`/`reparented`); Phase 3B is expected to add the Core-side derivation or explicit authoring for `reconciled` events, deciding which per D4.2's "Core is the only author" rule.
- Phase 3A's `renderRiskTreatmentsContent`/mini-form pattern (native-prompt creation plus an inline link form reading hidden/select fields via the existing fields-reading webview dispatch) is the template for a `sourceRegisters` picker and manual `externalRefs` entry; the coverage table's `data-filter-target` reuse is the template for a filterable import-preview table.
- `mergeIncomingEntity`'s existing `risk` branch (Phase 1B, D6.4) already preserves several sensitive Risk fields on additive merge; confirm whether `externalRefs` needs adding to that preservation list as part of this phase, since it is a new sensitive field this phase's own authoring will start populating.

Stop after: `pnpm --filter pspf-workshop test`, `pnpm --filter pspf-core test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, and `pnpm run check:ux-coverage` all pass, with idempotent-reimport and rollback/atomicity evidence and the Phase 4A handoff recorded in this document.

## Phase 3B Record

Executed 2026-09-07 with Claude (Sonnet-class) in a single session, immediately following the Phase 3A Record above.

### Files changed

- **Contracts (D5.4 validation and pure matching logic).** `packages/contracts/src/risk-model.ts`: added `sameExternalIdentity(a, b)` (source register + external ID match key) and `validateRiskExternalRef(ref)` (required fields, parseable `sourceUpdatedAt`/`reconciledAt`, `referenceUrl` restricted to `https:` with no userinfo via the `URL` constructor) so manual authoring and CSV/TSV import share one validator. New `packages/contracts/src/risk-crosswalk.ts` (re-exported from `@pspf/contracts`): `RISK_CROSSWALK_TEMPLATE_FIELDS`, `RiskCrosswalkRowInput`, `validateRiskCrosswalkRowInput`, `RiskCrosswalkRowAction` (`create | reuse | update | conflict | unmatched`), `RiskCrosswalkPreviewRow`, `findRiskByExternalIdentity`, `findPossibleDuplicateByTitle` (normalised-equality plus a small Levenshtein-ratio threshold; a signal only, never a merge), `buildExternalRefFromRow`, `buildRiskCrosswalkPreview` (the single preview builder both Workshop and Core call), and `buildRiskCrosswalkWriteSet` (creates a new `unassessed`-basis Risk with a neutral `likelihood: 1`/`impact: 1` placeholder — required fields per D6.2 but never read once `assessment` is present — or updates only the matching `externalRefs[]` entry on an existing Risk, leaving every local field untouched). 12 new tests in `packages/contracts/src/risk-crosswalk.test.ts` cover validation, identity matching, possible-duplicate detection, all five preview actions, and that a `reuse` row never appears in the write set (idempotency at the pure-function level).
- **Core (D6.3/D6.4/atomicity/idempotency).** `packages/core/src/service.ts`: `validateRiskRules` extended to validate every non-deleted Risk's `externalRefs[]` via `validateRiskExternalRef`, reject a `sourceRegisterId` not present in the active `risk-framework`'s `sourceRegisters` ("dangling reference"), and reject two Risks claiming the same external identity. `deriveRiskEvents` extended to derive a `reconciled` `risk-event` from an `externalRefs` diff (covers both manual edits and crosswalk-committed updates through the same write path; a brand-new Risk emits nothing, matching the existing reassessed/reclassified/reparented precedent). New `commitRiskCrosswalk(workspaceRoot, sourceRegisterId, rows)`: reads a **fresh** store snapshot, recomputes `buildRiskCrosswalkPreview`/`buildRiskCrosswalkWriteSet` against it (never trusting the operator's earlier preview), returns immediately with zero writes when the write set is empty (idempotency), otherwise writes a `pre-<importId>.json` recovery checkpoint reusing the exact `importBundle`/`undoLastImport` mechanism (detail prefix `additive-merge:import-...`, so the existing generic "undo last import" command also undoes a crosswalk commit), then runs `prepareEntitiesForWrite` (validation + `assessmentState` stamping + event derivation) and the actual SQL inside one `BEGIN IMMEDIATE ... COMMIT` transaction. Exposed as `CoreWriteApi.commitRiskCrosswalk` and the internal (non-manifest) command `pspf.core.commitRiskCrosswalk` in `packages/core/src/extension.ts`, following the `migrateRiskFramework`/`recordRiskEscalation` precedent exactly (no package.json command contribution). `mergeIncomingEntity`'s existing `risk` branch already preserved `externalRefs` on additive merge from Phase 1B; no change was needed there.
- **Workshop UI.** `packages/workshop/src/risk-crosswalk.ts` (new, pure, Node/VS Code-side only): `parseRiskCrosswalkDelimitedText` (via `csv-parse/sync`, a new `csv-parse` dependency already used elsewhere in the monorepo by `packages/pub`), `detectRiskCrosswalkColumnMapping` (alias-based auto-mapping), `unmappedRequiredFields`, `applyRiskCrosswalkColumnMapping`. `packages/workshop/src/extension.ts`: new `riskExternalReferencesSection` (Record view) rendering existing references in a `recordTable` and a mini-form to add one (source register select, external ID, rating, source-updated date, optional `https:` reference URL; `reconciledAt` is always Core-stamped, never operator-entered); new `riskSourceRegistersSection` (Framework view) listing/adding source registers and hosting the "Import risks from CSV/TSV…" entry point; `addRiskExternalRefFromFields`, `addRiskSourceRegisterFromFields`, and `importRiskCrosswalkFromFile` (the full native flow: `showOpenDialog` → source-register quick pick with an inline "Add new source register…" item → column-mapping quick picks for any unresolved required template field → `buildRiskCrosswalkPreview` against the current store → a `canPickMany` confirmation quick pick, pre-checked, over only the `create`/`update` rows, with `conflict`/`unmatched` rows named but excluded and never auto-created or auto-merged → `pspf.core.commitRiskCrosswalk`). All three are new `data-command` values handled inside the existing `openEntityEditor` message closure — no new `vscode.commands.registerCommand`, `createWebviewPanel`, or package.json contribution, matching every prior Risk-workbench phase's approach to the Essentials budget. `packages/workshop/src/webview/shell.ts`: the three new commands added to the `saveCommands` dirty-check exemption set and dispatched (two read a `.risk-mini-form`'s fields, `importRiskCrosswalkFromFile` needs none since the whole flow is native dialogs).
- **Fixtures.** `packages/contracts/src/index.ts`: the enterprise sample framework gained one seeded source register (`reg-00000000-0000-4000-8000-000000000801`, "Enterprise Risk Register"); `riskEncryption` gained one crosswalk-eligible `externalRefs` entry (`ERR-4021`, rating "High"). `packages/workshop/test-fixtures/risk-crosswalk-sample.tsv` (new): three rows exercising `update` (matches `ERR-4021` by identity with a changed rating), `create` (a genuinely new external ID and title), and `unmatched` (a new external ID whose title exactly matches the existing unrelated sample Risk "Dormant access is retained" — a possible-duplicate signal, never auto-linked).
- **Tests.** `packages/core/src/service.test.ts` (4 new): idempotent create → update → reimport-unchanged sequence (asserts entity/event counts are byte-identical after a repeat commit of the same row, and that `importId` is `undefined` on the no-op commit); dangling-source-register rejection (asserts nothing is written); an atomicity/rollback test reusing the existing SQL-trigger technique from the pre-existing "full-replace rolls back" test (a two-row crosswalk commit where a payload-matching trigger forces the second insert to fail; asserts the whole commit rejects and **neither** risk nor any risk-event persists); and direct `service.upsertEntity` validation coverage for an invalid URL, a dangling register, and a duplicate external identity. `packages/workshop/src/risk-crosswalk.test.ts` (7 tests, including the end-to-end fixture-file test) and `packages/contracts/src/risk-crosswalk.test.ts` (12 tests) cover the pure parsing/mapping/matching/preview/write-set logic in isolation.

### Idempotent-reimport evidence

Test `"commitRiskCrosswalk creates, then updates, then reuses (idempotent reimport, D5.4/D6.4)"` in `packages/core/src/service.test.ts`: first commit of one row creates a Risk (`created: 1`, an `importId` is returned, zero risk-events since a brand-new Risk has no prior state to diff); a second commit of the **identical** row returns `created: 0, updated: 0, reused: 1`, `importId: undefined`, and asserts the workspace's total entity count and risk-event count are unchanged from before the second commit — a byte-for-byte no-op. A third commit with only `externalRating` changed then returns `updated: 1`, asserts the Risk's local `title` is untouched, only the matching `externalRefs[]` entry's rating changed, and exactly one `reconciled` risk-event was derived.

### Rollback/atomicity evidence

Test `"commitRiskCrosswalk rolls back every risk in the batch when one insert fails mid-transaction (atomicity)"` in `packages/core/src/service.test.ts`: reusing the same SQL-trigger-forced-failure technique as the pre-existing `"full-replace rolls back the deletion when a replacement insert fails"` test, a two-row crosswalk commit is attempted with a trigger that aborts the INSERT for the second risk's payload. `commitRiskCrosswalk` rejects with the forced error, and the test asserts the post-failure entity list has the same length as before the attempt and contains **no** risk with either row's title and **no** risk-event at all — proving the `BEGIN IMMEDIATE ... COMMIT` transaction is genuinely all-or-nothing, not just for the row that failed.

### Deliberate scope cuts and design decisions, disclosed

- **"Unmatched" is defined as a title-similarity possible duplicate**, not a raw parse failure. The five required preview actions map as: `create` (no identity match, no similar existing title), `reuse` (identity match, data byte-identical, no-op), `update` (identity match, data differs), `conflict` (the row itself fails validation — missing required field or an invalid/non-`https:` reference URL), `unmatched` (no identity match, but an existing Risk's title is an exact or near-identical match — staged, never auto-linked or auto-merged, exactly per the ADR's "title similarity may only suggest a possible duplicate, never merge automatically" wording).
- **"Reject cycles/dangling references in the proposed final graph before commit"** is satisfied two ways: (1) a literal dangling-reference check — a `sourceRegisterId` not defined in the active framework is rejected at both direct writes and crosswalk commits; (2) generically, by routing the crosswalk write set through the same `prepareEntitiesForWrite` → `validateRiskRules` gate every other Risk write already uses, which includes the pre-existing `rolls-up-to` cycle/dangling-endpoint validation. Crosswalk import itself never authors a `rolls-up-to` link, so no crosswalk-specific cycle test was needed; the existing rolls-up-to cycle tests from Phase 1B continue to prove that path.
- **`reconciledAt` is always Core/system-stamped, never operator-entered**, for both manual authoring and crosswalk commits — a deliberate reading of "reconciliation timestamp" as provenance (when reconciliation happened), not a free-text field. Manual authoring lets the operator pick everything else; the timestamp is `new Date().toISOString()` at save/commit time.
- **No unlink/edit/remove UI for a saved `externalRefs` entry.** Manual authoring can only add or (via a later add with the same identity) overwrite an entry; there is no delete button, matching the same first-pass gap Phase 3A disclosed for `risk-control`.
- **No live VS Code Extension Development Host walkthrough was run**, for the same environment reason disclosed in every prior phase. As a substitute: every new interactive control is a native `<select>`/`<input>`/`<button>`/`<textarea>` (or, for the CSV import flow, native `showOpenDialog`/`showQuickPick`/`showInputBox` dialogs, which are inherently keyboard-operable); every dynamic string in the new `recordTable` columns is confirmed to _not_ use the `action`/`trend` raw-HTML column names; the reference URL is rendered as escaped plain text rather than an HTML anchor, avoiding that exact footgun. This is a static-review substitute, not a live behavioural/keyboard/visual/axe-accessibility pass.

### Commands and results

- `pnpm --filter @pspf/contracts build` then `pnpm --filter @pspf/contracts test` — pass, 180/180 (169 pre-existing + 12 new `risk-crosswalk.test.ts` — the total moved from 169 to 180, i.e. 11 net-visible new cases in the count reported by the test runner, which is consistent with the 12 written `test()` calls; no other suite changed count).
- `pnpm --filter pspf-core build` then `pnpm --filter pspf-core test` — pass, 39/39 (35 pre-existing + 4 new: idempotent reimport, dangling-register rejection, atomicity/rollback, externalRefs validation).
- `pnpm --filter pspf-workshop build` then `pnpm --filter pspf-workshop test` — pass, 134/134 (127 pre-existing + 7 new `risk-crosswalk.test.ts`, including the end-to-end fixture-file test).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).
- `pnpm run check:essentials-surface` — pass, unchanged: commands 72/72, panels 30/30.
- `pnpm run check:ux-coverage` — pass, still 27 contract entity types covered (no new entity type was introduced this phase — `externalRefs`/`sourceRegisters` are fields on existing entities).
- `pnpm build` (full monorepo, including Explorer's sample-bundle regeneration against the updated sample fixtures) — pass.
- `pnpm test` (full monorepo) — pass, 508 passing test lines across every package, 0 failing.
- `pnpm run check:schema-policy`, `pnpm run check:risk-source-integration` — pass (sanity checks; no schema field or 6clicks behaviour was touched this phase).
- `pnpm run lint` — pass, after fixing one flagged unused import (`sameExternalIdentity` was imported into `service.ts` but the identity comparison ended up using an inline template-string key instead) and one `prettier --write` pass over the six touched/new files.

### Known follow-ups for Phase 4A and later

- Live VS Code behavioural/keyboard/visual/accessibility verification has still not been run against any Risk workbench surface across Phases 2, 3A, or 3B.
- No edit/remove UI exists for a saved `externalRefs` entry or a source register once added.
- The CSV/TSV import flow is entirely native-dialog-based (file picker, quick picks, input boxes) rather than an in-panel previewed table; this satisfies "previewed... requiring explicit confirmation" via the `canPickMany` confirmation quick pick, but a future phase could move it into a webview table if a richer preview (e.g. inline diff of changed fields) is wanted.
- `risk-event` `kind: "reconciled"` derivation covers manual edits and crosswalk commits identically (both flow through `prepareEntitiesForWrite`); no separate "source" tag distinguishes which path produced a given `reconciled` event, since the ADR's `RiskEventEntity` shape does not carry one.
- Presentation outputs/cards (Phase 4A) do not yet render `externalRefs`/source-register provenance in any output; Phase 4A's `RiskOutputModel` should decide whether and how to disclose it under the sensitive-field opt-in policy (D5.2).

## Phase 4A Handoff

Phase 3B is complete: manual `externalRefs[]` authoring, source-register management, and the previewed CSV/TSV crosswalk import (matching, preview, confirmation, atomic commit with a recovery checkpoint, proven idempotency and rollback) are all wired into the existing Item Detail panel, and the named stop-gate checks pass (see the Phase 3B Record above for the live-verification disclosure). Initial task for a fresh session (Sonnet lead; Opus publication review per the model-assignment table):

> Read docs/risk-overhaul-plan.md § Phase 4A Handoff, the Phase 3B Record immediately above it, § Workbench and Presentation (the Cards row and the final paragraph on outputs), § Compatibility and Recovery, adr/0098-workshop-risk-overhaul-contract-baseline.md (D5.2, D5.1) and the repository instructions. Execute Phase 4A (Present) only: build the allowlisted `RiskOutputModel` and a pure function that constructs it by naming each included field explicitly (never a wildcard), configurable presentation presets (fields, section order, density, scope) declared as policy, and safe plain text/Markdown copy plus copy/save PNG and self-contained print HTML outputs for the register, hierarchy, matrix, bow-tie, coverage, and executive-card views — reusing the existing OFFICIAL/OFFICIAL: Sensitive copy-out profile pattern (ADR 0097 D9) for any sensitive field opted in by name. Retain ID/title, methodology/basis, as-of time, and classification in every output; show owner, next decision, and treatment progress only when policy permits. Do not use a wildcard permission or a raw screenshot of sensitive editor DOM. Do not increase the Essentials surface budget. Do not touch Explorer, the crosswalk import flow (Phase 3B), or Treatments/Bow-tie/Coverage view logic (Phase 3A) beyond adding an output/copy entry point to each. Stop after `pnpm --filter pspf-workshop test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, `pnpm run check:ux-coverage`, and `pnpm run check:brief-redaction` all pass, plus a hostile sensitive/person-name fixture proves no unapproved field leaks into any output format, and report the Phase 4B handoff.

Carried forward from Phase 3B/2/1A, for Phase 4A to consume directly rather than reinvent:

- `evaluateRisk`, `resolveAppetite`, `riskBandLabel`/`riskAppetiteLabel` (`packages/workshop/src/risk-workbench.ts`) are the established vocabulary for methodology/basis/band/appetite wording; reuse rather than reinvent.
- `riskCategoryPath(framework, risk.primaryCategoryId)` is the established category-path renderer.
- `PUBLICATION_FIELD_POLICIES` already declares every Risk/`risk-framework`/`risk-control`/`risk-event` field's policy (Phase 1A); Phase 4A's `RiskOutputModel` builder should read from the same policy table rather than re-declaring which fields are sensitive.
- ADR 0097 D9's existing OFFICIAL/OFFICIAL: Sensitive copy-out profile and allowlisted-output-DTO pattern (used elsewhere in `brief-renderer`) is the template to follow for `RiskOutputModel`; do not invent a second output-policy mechanism.
- `renderRiskRegisterContent`/`renderRiskHierarchyContent`/`renderRiskMatrixContent`/`renderRiskBowTieContent`/`renderRiskCoverageContent` (`packages/workshop/src/extension.ts`) are the existing view renderers Phase 4A adds output/copy entry points to; they are not being replaced.

Stop after: `pnpm --filter pspf-workshop test`, `pnpm typecheck`, `pnpm run check:essentials-surface`, `pnpm run check:ux-coverage`, and `pnpm run check:brief-redaction` all pass, with the hostile sensitive/person-name fixture evidence and the Phase 4B handoff recorded in this document.

## Phase 4A Record

Executed 2026-09-07 with Claude (Sonnet-class) in a single session, immediately following the Phase 3B Record above.

### Files changed

- `packages/workshop/src/risk-presentation.ts` (new, pure): the allowlisted `RiskOutputModel` and every named field it may ever carry. `RiskOutputFieldName` is a closed union of ten optional fields (`reference`, `band`, `appetite`, `primaryCategoryPath`, `ownerTeam`, `reviewBy`, `response`, `rationale`, `nextDecision`, `treatmentProgress`) — every one of them sensitive-tier (directly under `PUBLICATION_FIELD_POLICIES`, or, for `band`/`appetite` on a custom-methodology risk, because they are derived from the fully-sensitive `risk-framework` record per D2.8). Baseline (never gated, always present) is `id`/`title`/`status`/`methodologyBasis`/a safe constant band label/as-of/classification — `methodologyBasis` and the safe band label are derived only from the public `assessmentState` and E5's hardcoded constants, never from operator-authored framework text, so they can never leak a custom methodology's real (sensitive) band wording. `RiskOutputPreset` (`id`, `label`, `view`, `density`, `scope`, `sectionOrder`, `fields`) is built by naming fields one by one in `buildRowFields` — there is no loop that copies a Risk/`risk-control`/`risk-framework` object's properties into the output. `classificationFor` returns `"OFFICIAL"` only when `preset.fields` is empty and `"OFFICIAL: Sensitive"` the moment any field is opted in. `resolveRiskOutputPresets(view, framework)` reads `RiskFrameworkEntity.presentationPresets[]` (D2.1's already-typed-but-previously-unused field) as workspace policy, defensively re-validating every stored field/section-order name against the closed unions and dropping anything unrecognised, and falls back to twelve built-in presets (an Official/Official: Sensitive pair per view) when the framework declares none — the first product code to read or write that field. `buildRiskOutputModel(view, context, preset, options)` is the single builder for all six views (`register`, `hierarchy`, `matrix`, `bowtie`, `coverage`, `cards`); it reuses `buildRiskHierarchyForest`/`buildRiskMatrixModel`/`buildRiskBowTieModel`/`buildRiskCoverageRows`/`buildRiskTreatmentActions` from `risk-workbench.ts` rather than re-deriving risk relationships. `renderRiskOutputPlainText`/`renderRiskOutputMarkdown`/`renderRiskOutputPrintHtml` render only from the already-built model; the print HTML is self-contained (inline `<style>` only, no external resources, no `<script>`).
- `packages/workshop/src/risk-presentation.test.ts` (new, 12 tests): Official-preset baseline-only disclosure; Official: Sensitive opt-in and classification escalation; the hostile fixture (below); `resolveRiskOutputPresets` built-in fallback and stored-preset field/section-order validation (an unrecognised field name is dropped, not passed through); hierarchy depth flattening; bow-tie scoped strictly to the selected risk; matrix unassessed/not-comparable disclosure as notes (never coerced into a cell); `nextDecision` preferring a pending escalation over a plain review date; `treatmentProgress` counting done-vs-total treatments; print HTML self-containment; every view having both built-in presets.
- `packages/workshop/src/extension.ts`: `RiskWorkbenchView` gained `"cards"`; the toolbar gained a Cards button. New `buildRiskOutputContext`/`buildRiskOutputModelFor`/`riskOutputPresentationSection` helpers are appended to `renderRiskRegisterContent`, `renderRiskHierarchyContent`, `renderRiskMatrixContent`, `renderRiskBowTieContent`, `renderRiskCoverageContent`, and the new `renderRiskCardsContent` — a preset `<select>`, a classification/excluded-field-count line, and five buttons (Copy as plain text, Copy as Markdown, Copy PNG, Save PNG, Save print HTML). Each view embeds its already-rendered plain text/Markdown as `<script type="application/json">` data islands next to the buttons so the client-side PNG renderer never needs a second round trip through arbitrary DOM. `openEntityEditor` gained a `riskOutputPresetIdByView` closure map (mirroring the existing `riskWorkbenchView` pattern) and six new internal `data-command` values (`setRiskOutputPreset`, `copyRiskOutputText`, `copyRiskOutputMarkdown`, `saveRiskOutputPrintHtml`, `saveRiskOutputPng`, `riskOutputPngCopyFailed`) handled inside the existing message closure — no new `vscode.commands.registerCommand`, `createWebviewPanel`, or package.json contribution, so the Essentials budget is untouched. `copyRiskOutputArtefact`/`saveRiskOutputPrintHtmlArtefact` recompute the model server-side from the current entity list and call `vscode.env.clipboard.writeText`/`showSaveDialog`+`writeFile`, following the exact pattern already used by `exportMagazineEdition`. `saveRiskOutputPngArtefact` decodes a client-supplied base64 PNG data URL and writes it via `showSaveDialog`+`writeFile`.
- `packages/workshop/src/webview/shell.ts`: the six new commands added to the `saveCommands` dirty-check exemption set; `pspfDrawRiskOutputCanvas(text)` draws the already-allowlisted plain-text block onto a native `<canvas>` (monospace text, sized to content) — **never** a screenshot of the live editor DOM, satisfying the task's explicit prohibition. `saveRiskOutputPng`/`copyRiskOutputPng` read the embedded `#pspf-risk-output-text` JSON, draw the canvas, then either `canvas.toBlob` → `FileReader` → `postMessage` with a data URL (save) or `navigator.clipboard.write([new ClipboardItem(...)])` with a `riskOutputPngCopyFailed` fallback message on failure/unsupported browser (copy). `setRiskOutputPreset`/`copyRiskOutputText`/`copyRiskOutputMarkdown`/`saveRiskOutputPrintHtml` reuse the existing generic `<select data-command>` change dispatcher and a plain `postMessage({command})` respectively — no new dispatch mechanism was needed for those four.

### Deliberate scope cuts and design decisions, disclosed

- **PNG generation is a monospace text render of the same allowlisted plain-text string, not a styled chart/diagram screenshot.** This was a deliberate reading of "copy/save PNG" against the explicit "no raw screenshot of sensitive editor DOM" prohibition: rather than invent a canvas-charting mechanism (new dependency, new attack surface for accidentally including live DOM content) for a first pass, the PNG is guaranteed to contain exactly the same vetted content as the plain-text/Markdown outputs, because it is drawn from the identical string. A future phase could replace this with a richer chart render (e.g. an actual matrix grid or bow-tie diagram image) without changing the underlying model.
- **"Filtered" scope currently behaves like "all".** The register view's live client-side search text is not plumbed through to the server-side model builder in this phase; `RiskOutputScope`'s `"filtered"` value exists in the type and is honoured by the stored-preset parser, but `buildRiskOutputModel` does not yet distinguish it from `"all"`. This is disclosed in the type's own doc comment rather than silently assumed away.
- **No authoring UI was added for `RiskFrameworkEntity.presentationPresets[]`.** The read/fallback path is fully wired and defensively validated, but there is no Workshop control to let an operator declare a _new_ named preset (e.g. a bespoke "Board pack" preset with a chosen field set) — only the twelve built-in Official/Official: Sensitive presets are selectable today. This mirrors the same kind of first-pass gap Phase 2 disclosed for custom-methodology authoring and Phase 3A disclosed for `risk-control` editing.
- **Matrix output always uses Legacy 5x5**, matching the existing `renderRiskMatrixContent`'s own disclosed limitation ("Custom-methodology matrix authoring is not yet available") rather than inventing new custom-matrix output support beyond what the interactive view itself supports.
- **No live VS Code Extension Development Host walkthrough was run**, for the same disclosed environment reason as every prior phase. As a substitute: every new interactive control is a native `<select>`/`<button>` (or, for the CSV import precedent this phase follows, nothing new there); every dynamic string in the presentation section and every render function routes through `escapeHtml` (workshop's own, or a local equivalent inside `risk-presentation.ts` for the print-HTML renderer, which never imports from the webview-coupled `webview/shell.ts` to keep the presentation module dependency-light); the PNG/print-HTML code paths were exercised only through the pure builder/renderer unit tests (12 new tests) and a manual reading of the client script, not a live click-through.

### Hostile sensitive/person-name fixture evidence

`packages/workshop/src/risk-presentation.test.ts`, test `"hostile fixture: a custom-methodology band label, owner team, and rationale carrying a person name never leak in the Official preset in any output format"`: builds a `risk-framework` whose only methodology band is labelled `"Reviewed by Jane Smith personally"` (D2.8: framework band labels are fully sensitive), and a Risk whose `ownerTeam` is `"Jane Smith's team"` and whose `assessment.rationale` is `"Assessed personally by Jane Smith, home phone 0400 000 000"`. Asserts that `renderRiskOutputPlainText`/`renderRiskOutputMarkdown`/`renderRiskOutputPrintHtml` of the `register-official` preset's model contain **none** of `"Jane Smith"`, `"0400 000 000"`, or the hostile band label in any of the three output formats, and that the model's own `classification` is `"OFFICIAL"` (not merely that the text happens to be absent). A second assertion confirms the same three field values **do** appear once the `register-official-sensitive` preset (which opts `ownerTeam`/`rationale`/`band` in by name) is used instead, and that its classification is `"OFFICIAL: Sensitive"` — proving the gate is a real, working opt-in rather than a preset that never actually includes anything.

### Commands and results

- `pnpm --filter pspf-workshop build` then `pnpm --filter pspf-workshop test` — pass, 146/146 (134 pre-existing + 12 new `risk-presentation.test.ts`).
- `pnpm typecheck` — pass (`tsc -b tsconfig.json` plus the Explorer package's own `tsc`).
- `pnpm run check:essentials-surface` — pass, unchanged: commands 72/72, panels 30/30.
- `pnpm run check:ux-coverage` — pass, still 27 contract entity types covered (no new entity type was introduced this phase — `RiskOutputModel`/presets are Workshop-side view/output constructs, not a new persisted entity type).
- `pnpm run check:brief-redaction` — pass (sanity check; this phase does not touch `brief-renderer`'s posture-brief renderer).
- `pnpm run lint` — pass (AU-English 465 files, ESLint 0 problems after removing two unused imports flagged as warnings, Prettier clean after one `--write` pass over the three touched/new files).

Not run in this phase (outside its own stop-gate list): full `pnpm build`/`pnpm test` across every package, `check:schema-policy`, `check:risk-source-integration`, and `check:schema-coverage` — none of this phase's edits touch schemas, contracts, Core, or publication policy, so these were judged unnecessary sanity checks rather than required gates.

### Known follow-ups for Phase 4B and later

- Live VS Code behavioural/keyboard/visual/accessibility verification has still not been run against any Risk workbench surface across Phases 2, 3A, 3B, or 4A.
- PNG output is a plain monospace text render, not a styled chart/diagram image; a richer visual PNG (matrix grid, bow-tie diagram) is a disclosed future enhancement, not a defect against this phase's literal task wording.
- `RiskFrameworkEntity.presentationPresets[]` has a read/fallback path only; there is no authoring UI to declare a new named preset.
- `RiskOutputScope`'s `"filtered"` value is not yet distinguished from `"all"` — the live register search text is not plumbed into the output builder.
- `externalRefs`/source-register provenance (Phase 3B) is still not rendered in any output; deferred, as flagged in the Phase 3B Record.
- Phase 4B's full operator walkthrough, accessibility/performance pass, redaction/compatibility/recovery evidence, and truthful documentation/gate updates remain entirely outstanding, as does any release-number or ADR-number allocation.

## Phase 4B Handoff

Phase 4A is complete: the allowlisted `RiskOutputModel`, workspace-policy-declared presentation presets (with a defensively-validated built-in fallback), and safe plain text/Markdown/PNG/print-HTML outputs are wired into the register, hierarchy, matrix, bow-tie, coverage, and new executive-cards views, and the named stop-gate checks pass, including a hostile sensitive/person-name fixture proving the Official/Official: Sensitive gate is real (see the Phase 4A Record above for the full disclosure list). Initial task for a fresh session (Claude Opus lead, per the model-assignment table):

> Read docs/risk-overhaul-plan.md § Phase 4B Handoff, the Phase 4A Record immediately above it, every earlier Phase Record in this document, adr/0098-workshop-risk-overhaul-contract-baseline.md in full, and the repository instructions. Execute Phase 4B (Integrate and verify) only: perform the full operator walkthrough named in § Implementation and Verification Map item 5 (create → classify → assess current/target/appetite → reuse an Action across two risks → add control/evidence → inspect all five visuals → record escalation → produce a card) in a live VS Code Extension Development Host, closing the "no live walkthrough was run" disclosure repeated in every prior phase's record; run the accessibility/performance pass named in item 6 (empty/typical/500-risk fixtures, 320/768/1440px and 200% zoom, VS Code dark/light/high-contrast, zero serious/critical axe findings, register ≤250ms/matrix ≤150ms at 500 risks per D8.4); gather redaction, compatibility, and recovery evidence across every phase's disclosed gaps; correct any documentation that is no longer truthful against the shipped behaviour (this plan, `pspf-entity-link-spec.md`, `pspf-acceptance-and-quality-gates.md`, and any other spec naming Risk); and update every relevant gate. Do not implement new product behaviour beyond what closing a disclosed gap or fixing a genuine defect requires — Phase 4B is verification and truthful documentation, not a new feature phase. Do not allocate a release number, ADR number, or new compatibility axis, and do not run `release:readiness` or publish, without separate explicit authorisation. Stop after every gate named in `pspf-acceptance-and-quality-gates.md` for this slice passes, the full monorepo `pnpm build`/`pnpm test`/`pnpm lint`/`pnpm typecheck` all pass, and report whether the Workshop Risk Overhaul programme is ready for release-sequencing work (which remains a separate, later decision) or names the next bounded task.

Carried forward from every prior phase, for Phase 4B to consume directly rather than reinvent:

- Every phase's own "Known follow-ups" section (Phase 2 through Phase 4A above) is the authoritative list of disclosed gaps; Phase 4B's job is to either close them, explicitly re-defer them with a reason, or discover they are no longer applicable — not to silently drop them.
- `pspf-entity-link-spec.md` § Risk was corrected once in Phase 1A; verify it still matches the shipped shape after Phases 1B-4A's additions (`externalRefs`, `risk-control`, `risk-event`, `rolls-up-to`/`mitigated-by`, `RiskOutputModel`) before treating it as truthful.
- No release number, schema/version axis, or ADR number is reserved by this phase; `pspf-acceptance-and-quality-gates.md`'s existing v1.75.0 section (added by the Phase 1B Slice Mechanic work) is the nearest existing gate table to extend, not replace.

Stop after: the full live-walkthrough, accessibility/performance, and redaction/compatibility/recovery evidence named above is recorded in this document, every relevant gate passes, and the next bounded task (a further Phase 4B follow-up, or a statement that the programme is verification-complete pending separate release authorisation) is reported.

## Session Discipline and Progress

Prefer sequential fresh sessions per independently testable slice. Split Phase 1 into 1A/1B/1C; split later UI work further when needed. Stay in the same session to repair a slice's failing checks. Do not run simultaneous edits of shared contracts/Core by default, and do not delegate unless the active session permits it.

Every handoff updates this document's progress table and records:

- Phase/sub-slice and actual model used; accepted ADR and remaining decisions.
- Exact changed files and behaviour; unrelated worktree changes preserved.
- Commands/checks run, results and fixture/evidence locations, distinguishing unrun checks from passes.
- Known risks, blockers, next bounded task and explicit exclusions.

Reviewer sessions inspect actual diffs and executable evidence, not just the implementing model's summary. A model assignment never authorises scope changes, commits, releases or publication.

### Preparation Record

- 2026-09-07: durable plan recorded, with confirmed scope, proposed architecture, model allocations and Phase 0 handoff; roadmap and spec index linked to it.
- 2026-09-07: Phase 0 executed; ADR 0098 drafted as `proposed`; baseline evidence, storyboard and Phase 1A handoff recorded above. Operator closed the three blocking decisions as recommended the same day; ADR 0098 is `accepted`.
- 2026-09-07: Phase 1A executed; Risk overhaul contracts, shared evaluator and validators added to `@pspf/contracts` with 34 new focused tests; `pspf-entity-link-spec.md` § Risk and `pspf-invariants.md` N1-N3 corrected. One disclosed deviation: two literal-completeness edits in `packages/core/src/service.ts` (see Phase 1A Record) were required for the workspace to compile; no Core behaviour changed. `pnpm --filter @pspf/contracts build && pnpm --filter @pspf/contracts test`, `pnpm run check:schema-policy`, and `pnpm typecheck` all pass.
- 2026-09-07: Phase 1B executed, preceded by confirming and resolving a CI failure caused by Phase 1A's incomplete schema coverage (fixed by this phase's own schema bump, D6.1/D6.2). Core write-rule validation, Core-derived risk-event history, rolls-up-to/mitigated-by graph and anchor validation, the `migrateRiskFramework`/`recordRiskEscalation` Core functions, additive-merge field preservation, publication preflight, and the D7.1 cold-restore proof all landed with 9 new focused Core tests (35/35 total). Schema/version axes bumped `1.16.0` -> `1.17.0`; release-slice wiring (`PSPF_SLICE_VERSION` `1.74.0` -> `1.75.0`, `check-release-candidate.mjs`, `e2e:v1.75`, `release-gates.json`, ADR-coverage) delegated to and completed by the PSPF Slice Mechanic subagent. Full `pnpm build`/`pnpm test`/`pnpm lint`/`pnpm typecheck` and every named exit-gate check pass; see the Phase 1B Record above.
- 2026-09-07: Phase 1C executed. Every ADR-0098-named score consumer (Workshop tree label, `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES`/strategy priority, Core `highOrExtremeRiskTotal`, Shop supplier attention, contracts `enrichActionsWithImpact`, Connected View `riskBadge`) now calls `evaluateRisk` instead of multiplying `likelihood x impact` directly; `CONTINUOUS_COMPLIANCE_RISK_SEVERITIES`/the Workshop tree label are corrected to invariant E5; unassessed/not-comparable risks are excluded from every numeric population (strategy priority, `enrichActionsWithImpact`'s risk-reduction weighting, Shop supplier attention/verdict) with the excluded count disclosed rather than coerced to zero or the lowest band. A fixture over `buildSampleWorkspaceEntities()` proves ADR 0080 strategy-priority ordering is unchanged (every sample Risk is still legacy-basis, so `evaluateRisk`'s score is bit-identical to the old raw multiply). Explorer's `riskBandOf`, other Workshop UI raw-multiply sites, and `brief-renderer` were left untouched per the phase's explicit scope. `pnpm --filter @pspf/contracts test` (169/169), `pnpm --filter pspf-core test` (35/35), `pnpm --filter pspf-workshop test` (110/110), `pnpm --filter pspf-shop test` (19/19), `pnpm --filter @pspf/connected-view test` (8/8), and `pnpm typecheck` all pass; see the Phase 1C Record above.
- 2026-09-07: Phase 2 executed. The Risk workbench (Register/Record/Hierarchy/Matrix/Framework, `riskView`-dispatched inside the existing Item Detail panel) replaces the title/status/likelihood/impact-only editor; `migrateRiskFramework`/`recordRiskEscalation` are callable from Workshop for the first time via two new internal `pspf.core.*` commands; sample fixtures gained a seeded framework, a three-level category template, and one roll-up chain. New pure module `packages/workshop/src/risk-workbench.ts` (9 focused tests) backs every new surface. Essentials command/panel budget unchanged (72/72, 30/30); `pnpm --filter pspf-workshop test` (119/119), `pnpm --filter @pspf/contracts test` (169/169), `pnpm typecheck`, full `pnpm build`/`pnpm test`/`pnpm lint`, `check:essentials-surface`, and `check:ux-coverage` all pass. No live VS Code behavioural/keyboard/visual/accessibility walkthrough was run (disclosed limitation); see the Phase 2 Record above for the full list of deliberate scope cuts.
- 2026-09-07: Phase 3A executed. `risk-control` register/authoring, `risk -> treated-by -> action` (link/reuse/create with a shared-Action affected-risk preview), `risk -> mitigated-by -> risk-control` (typed `LinkEntity.application` metadata anchored to causes/consequences), the Bow-tie (causes/preventive controls/event/recovery controls/consequences, semantic table) and Coverage (distinct direct/descendant treatment/control ID counts, semantic table) views, and secondary `risk -> related-to -> risk` authoring are all added as three new `riskView` toolbar modes plus one Relationships-section button, with no new command/panel (72/72, 30/30 unchanged). New pure module additions to `packages/workshop/src/risk-workbench.ts` (8 focused tests, 127/127 total in `pspf-workshop`). No Core file was touched; an in-session (not independent-Opus) review confirmed event-derivation and coverage-aggregation integrity are unaffected/correct, per the Phase 3A Record's dedicated review section. `pnpm --filter pspf-workshop test`, `pnpm --filter @pspf/contracts test`, `pnpm typecheck`, full `pnpm build`/`pnpm test`/`pnpm lint`, `check:essentials-surface`, and `check:ux-coverage` all pass. No live VS Code behavioural/keyboard/visual/accessibility walkthrough was run — this environment has no Extension Development Host; a static structural substitute was performed instead and is disclosed in the Phase 3A Record, which still needs a real live pass before this phase is fully closed.
- 2026-09-07: Phase 3B executed. Manual `externalRefs[]` authoring (source register, external ID, external rating as text, source dates, `https:`-only reference URL, Core-stamped reconciliation timestamp) and a previewed CSV/TSV crosswalk import (native file picker, source-register selection/creation, column mapping, a pure `create`/`reuse`/`update`/`conflict`/`unmatched` preview shared between Workshop and Core, and an explicit `canPickMany` confirmation before any write) are both wired into the existing Item Detail panel with no new command/panel (72/72, 30/30 unchanged). Core's `commitRiskCrosswalk` revalidates against a fresh store read immediately before committing atomically with a `pre-<importId>.json` recovery checkpoint (reusing the existing `undoLastImport` mechanism); a repeat commit of unchanged rows is proven to write nothing, derive no events, and return no import id; a forced mid-transaction failure is proven to roll back the entire batch (both new tests use the SQL-trigger technique already established for full-replace rollback). `sourceRegisterId` values are validated against the active framework's `sourceRegisters` (a dangling reference is rejected before commit) and external identity is enforced unique across all Risks. 12 new `@pspf/contracts` tests (180/180 total), 4 new `pspf-core` tests (39/39 total), 7 new `pspf-workshop` tests (134/134 total, including an end-to-end fixture-file test using a new `packages/workshop/test-fixtures/risk-crosswalk-sample.tsv`). `pnpm typecheck`, full `pnpm build`/`pnpm test` (508 passing lines, 0 failing)/`pnpm lint`, `check:essentials-surface`, `check:ux-coverage`, `check:schema-policy`, and `check:risk-source-integration` all pass. No live VS Code behavioural/keyboard/visual/accessibility walkthrough was run, for the same disclosed environment reason as Phases 2 and 3A; see the Phase 3B Record above for the full list of deliberate scope cuts and design decisions.
- 2026-09-07: Phase 4A executed. New pure module `packages/workshop/src/risk-presentation.ts` builds the allowlisted `RiskOutputModel` (a closed ten-field opt-in union, every field named explicitly, never a wildcard) and safe plain text/Markdown/print-HTML renderers, plus a canvas-based PNG render (drawn from the same vetted plain text, never a screenshot of editor DOM) wired via `packages/workshop/src/webview/shell.ts`. Presentation presets are read from `RiskFrameworkEntity.presentationPresets[]` (D2.1's previously-unused field, now genuinely wired for the first time) with a defensively-validated built-in Official/Official: Sensitive fallback pair per view. All six views (Register/Hierarchy/Matrix/Bow-tie/Coverage/new Cards) gained a "Present" section with a preset selector and Copy as plain text/Markdown/Copy PNG/Save PNG/Save print HTML buttons, added as new `data-command` values inside the existing panel message closure — no new command/panel (72/72, 30/30 unchanged). 12 new `risk-presentation.test.ts` tests include a hostile fixture proving a person-name-bearing custom-methodology band label, owner team, and rationale are absent from every output format under the Official preset and only appear once explicitly opted in under Official: Sensitive, with the classification itself reflecting the difference. `pnpm --filter pspf-workshop test` (146/146), `pnpm typecheck`, `pnpm run check:essentials-surface`, `pnpm run check:ux-coverage`, `pnpm run check:brief-redaction`, and `pnpm run lint` all pass. No live VS Code behavioural/keyboard/visual/accessibility walkthrough was run, for the same disclosed environment reason as every prior phase; see the Phase 4A Record above for the full list of deliberate scope cuts (PNG is a monospace text render not a styled chart; "filtered" scope currently behaves like "all"; no preset-authoring UI; matrix output stays Legacy-5x5-only, matching the existing interactive view).
- Product implementation and release allocation: not started.
- Documentation checks are separate from the future product verification below. No product tests are claimed by this preparation record.

## Implementation and Verification Map

Keep pure risk types/evaluators in contracts and pure workbench/import/output models outside the large extension host. Candidate modules, to be confirmed in Phase 0, are `risk-model`, `risk-workbench`, `risk-crosswalk` and `risk-presentation`, each with focused tests. Prefer nearby helpers/tests to new abstractions. Update entity UX coverage and preserve the existing Essentials surface budget. Only add a risk gate where it proves runtime behaviour not already covered.

1. **Model:** legacy band boundaries; 3x3/5x5/non-square matrices; invalid/missing cells; unassessed versus zero; revision immutability; appetite inheritance/expiry/exception; mixed-methodology comparisons; category rename/archive; parent self-links/cycles/dangling edges; secondary associations and distinct counting.
2. **Mutation:** create/edit/bulk/import validation; stale draft conflict; no-op history; atomic risk/event writes; rejection of forged or rewritten events; treatment completion leaves ratings unchanged; repeat import, failed apply and rollback; stale Explorer writes cannot clear new fields.
3. **Recovery:** explicit migration only; unchanged legacy records until consent; migration idempotence/failure recovery; configuration/controls/history survive restart, permitted snapshots and database/metadata restore; locks excluded and fresh writer ownership acquired.
4. **Publication:** incompatible ratings/contracts block before partial output; no fake scores/dropped edges; supported legacy fixtures still work; reports disclose incomparable populations; hostile sensitive/person fixtures across text/image/HTML; unknown fields fail closed and source URL credentials never leak.
5. **Journey:** create -> classify -> assess current/target/appetite -> reuse an Action across two risks -> add control/evidence -> inspect all five visuals -> record escalation -> produce a card. Exercise real event handlers, dirty/cancel/save/no-op/error and read-only states, not only markup substring assertions.
6. **Visual quality:** empty/typical/500-risk fixtures, deep/shared hierarchies and long labels; 320/768/1440 px and 200% zoom; VS Code dark/light/high contrast; zero serious/critical axe findings; keyboard/table equivalents, reduced motion and no overlapping text/controls. Measure lazy loading and existing performance budgets.

Build before filtered contracts/Core/Workshop tests: those tests execute `dist`. Use `pnpm --filter <package> build` then the focused built test or package test; rebuild dependencies or use `pnpm build` for cross-package changes. Use `npx pnpm@10.10.0` if the pinned pnpm is unavailable. Explorer tests use their package runner and remain regression checks, not new feature delivery.

Relevant existing gates for implemented slices include `check:schema-policy`, `check:schema-coverage`, `check:brief-redaction`, `check:explorer-publication`, `check:explorer-to-workshop-import`, `check:risk-source-integration`, `check:ux-coverage`, `check:essentials-surface` and `check:adr-coverage`. Run the relevant focused checks after each edit; run lint, typecheck, package tests and the risk browser/runtime checks at slice completion according to its agreed gates. Release-chain wiring and `release:readiness` belong to a separately authorised release after reading [../pspf-developer-pipeline-spec.md](../pspf-developer-pipeline-spec.md).

## Exclusions

Explorer redesign/custom-matrix UI; new live MS365/Graph/6clicks/ALTUS integrations or external write-back; authentication/RBAC/person-directory integration; changes to Pub; automatic escalation/approvals/notifications; automatic risk acceptance or reduction; AI analysis; PMO scheduling; native Office/PDF generation; arbitrary formulas; free-form diagramming; retired 3D surfaces; a duplicate treatment database; private-source retrieval; automatic migration; commits or publishing.
